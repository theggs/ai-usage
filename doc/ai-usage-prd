# AIUsage — 产品需求说明（PRD）

**版本：** 0.2  
**日期：** 2026-03-19  
**状态：** 澄清后修订稿

---

## 1. 背景与问题

AI 编程助手（Claude Code、OpenAI Codex 等）普遍采用"套餐额度"或"API 用量"计费模式。开发者在日常使用中，无法便捷地感知剩余额度，只能在配额耗尽、工具报错后才发现问题，严重打断工作流。

AIUsage 旨在解决这一痛点：以最低感知成本，在操作系统菜单栏/系统托盘常驻显示各 AI 服务的剩余额度，并在额度不足时主动提醒。

---

## 2. 目标用户

- 重度使用 AI 编程辅助工具的个人开发者
- 同时订阅多个 AI 服务（Claude、OpenAI 等）并需要统一管理额度的用户
- 对开发工具有审美要求，偏好轻量、原生体验的 macOS / Windows 用户

---

## 3. 产品目标

| 目标 | 衡量指标 |
|------|---------|
| 零打断感知余量 | 打开菜单栏弹窗 < 1 秒即可看到所有服务余量 |
| 提前预警 | 低额度通知在额度耗尽前触达用户 |
| 轻量无感 | 内存占用 < 50 MB，CPU 空闲时 < 1% |
| 跨平台一致 | macOS 菜单栏与 Windows 系统托盘功能对等 |

---

## 4. 功能需求

### 4.1 菜单栏 / 系统托盘常驻显示

- 应用以图标形式常驻于 macOS 菜单栏或 Windows 系统托盘。
- 图标旁可选显示余量简短摘要，方便一眼扫视。大模型服务通常存在多个额度维度（如 5 小时滚动窗口、7 天周期等），用户可在设置中选择：
  - 默认显示最低余量，优先暴露当前最紧张的额度维度；
  - 仅显示某一维度（如只看 5 小时窗口余量）；
  - 同时显示多个维度（如 `42% / 78%`）；
  - 不显示（仅图标）。
- 点击图标弹出面板，展示所有已配置服务的余量状态。

### 4.2 余量面板

弹出面板需包含：

- 每个已配置服务的名称、图标、当前余量（百分比 + 绝对值，视 API 支持情况）；对于存在多个额度维度的服务，逐行或分组展示各维度余量。
- 上次刷新时间。
- 手动刷新按钮。
- 进入设置的入口。

**后期扩展：** 支持用户通过拖拽自定义服务在面板中的显示顺序。

### 4.3 低额度本地通知 *(后期功能)*

- 用户可为每个服务单独设置告警阈值（默认 20%）。
- 当余量低于阈值时，通过操作系统原生通知推送一次告警。
- 余量恢复正常后（如套餐重置），重置告警状态，下次低额度时可再次触发。
- 通知频率上限：同一服务同一计费周期内最多通知 3 次，避免轰炸。

### 4.4 多账户 / 多 API Key 支持

- 同一服务可配置多个账户（如个人 Claude Pro + 公司 Claude API Key）。
- 面板中对同一服务的多个账户分行展示，并标注账户别名。
- 支持账户的增删改及启用/禁用。
- 对于 Iteration 2 的 Codex CLI 接入，真实同步范围先限定为单个本地活跃 Codex 会话；多账户真实切换留待后续迭代。

### 4.5 开机自启

- 安装后默认开启开机自启（可在设置中关闭）。
- 自启后静默进入菜单栏/托盘，不弹出主窗口。

### 4.6 设置页面

- 对于 Iteration 2 的 Codex CLI 接入，设置页主要承担本地 CLI 同步说明、托盘摘要规则和桌面偏好设置；不要求用户手工录入 Codex 凭证。
- 各服务刷新间隔配置（默认 15 分钟，最短 5 分钟）。
- 低额度阈值配置。
- 菜单栏 / 托盘摘要显示规则（最低余量 / 5 小时窗口 / 7 天或周窗口 / 多维摘要 / 仅图标）。
- 开机自启开关。
- 语言设置（初期支持中文、英文）。

---

## 5. 支持的服务（路线图）

| 迭代 | 服务 | 数据来源 |
|------|------|---------|
| Iteration 1 | 框架（无实际服务） | — |
| Iteration 2 | Codex CLI | 本地 Codex CLI `app-server` 结构化额度读取 |
| Iteration 3 | Claude Code / claude.ai | 本地日志（`~/.claude/`）+ Anthropic Console API |
| Iteration 4+ | Gemini、其他 | 待定 |

---

## 6. 非功能需求

### 6.1 性能

- 冷启动时间 < 2 秒。
- 后台轮询不影响系统性能（使用系统定时器，非忙轮询）。

### 6.2 安全

- API Key 必须存储于系统钥匙串（macOS Keychain / Windows Credential Store），不得以明文写入配置文件。
- 对于 Codex CLI 路径，前端不得直接执行或解析 CLI 输出；CLI 调用、会话状态判断和额度归一化必须在宿主层完成。

### 6.3 隐私

- 不上报任何用量数据至 AIUsage 自身服务器（本地优先，无云同步）。
- 开源，用户可自行审计。

### 6.4 兼容性

- macOS 13 Ventura 及以上。
- Windows 10 22H2 及以上。

---

## 7. 技术选型

| 层次 | 选型 | 理由 |
|------|------|------|
| 应用框架 | Tauri 2.0 | 跨平台菜单栏/托盘原生支持，包体积小（< 5 MB），Rust 后端性能好 |
| 前端 | React + Tailwind CSS | 生态成熟，与 Tauri 集成顺畅 |
| 语言 | Rust（后端逻辑）+ TypeScript（UI） | Tauri 标准栈 |
| 安全存储 | `keytar`（Tauri 插件） | 跨平台封装系统钥匙串 |
| 本地通知 | Tauri Notification 插件 | 原生系统通知，无需额外依赖 |

---

## 8. 迭代规划

### Iteration 1 — 框架搭建（当前目标）

目标：搭建可运行的跨平台空壳，验证技术选型。

交付物：
- Tauri 2.0 项目初始化，macOS 菜单栏 + Windows 托盘均可运行。
- 弹出面板 UI 框架（含服务列表占位、设置入口）。
- 设置页面骨架（含 API Key 输入框、刷新间隔、自启开关）。
- 开机自启功能。
- 本地通知能力验证（发送测试通知）。
- CI/CD 流水线（GitHub Actions，分别构建 macOS `.dmg` 和 Windows `.msi`）。

不包含：任何真实 AI 服务的数据接入。

### Iteration 2 — OpenAI / Codex 支持

接入本地 Codex CLI 的结构化额度读取，在面板与托盘中展示真实 Codex usage limits，并支持托盘摘要规则设置。默认围绕单个本地活跃会话工作，不要求用户手工录入 Codex 凭证。

### Iteration 3 — Claude 支持

接入 Claude Code 本地日志解析 + Anthropic Console API，实现余量展示。

---

## 9. 开放问题

| # | 问题 | 负责人 | 优先级 |
|---|------|--------|--------|
| 1 | claude.ai 订阅额度是否有官方 API 可查？若无，是否降级为本地日志解析？ | 待调研 | 高 |
| 2 | Windows 系统托盘图标是否需要设计多套分辨率（16/32/64px）？ | 设计 | 中 |
| 4 | 是否需要支持自定义 API 端点（企业私有部署场景）？ | 产品 | 低 |

---

## 10. 术语表

| 术语 | 说明 |
|------|------|
| 额度（Quota） | 套餐内允许使用的资源上限，如 token 数、请求次数或计费金额 |
| 余量 | 当前计费周期内剩余的可用额度 |
| 计费周期 | 套餐的额度重置周期。不同服务、不同额度维度的周期各异，常见有：5 小时滚动窗口、7 天滚动窗口、自然月等，同一服务可能同时存在多个周期维度 |
| API Key | 用于调用 AI 服务 API 的身份凭证 |
| 托盘 | Windows 任务栏右侧的系统通知区域，等同于 macOS 菜单栏 |

---
phase: quick
plan: 260402-f56
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/promotions/catalog.ts
  - src/app/shell/AppShell.tsx
  - src/components/panel/PromotionStatusLine.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Codex limited-time promotion campaign is marked ended in the catalog"
    - "PromotionStatusLine is not rendered when no provider has active promotions"
    - "Tests pass with updated promotion state and hidden-when-empty behavior"
  artifacts:
    - path: "src/features/promotions/catalog.ts"
      provides: "Codex campaign marked ended with endsAt and historyNote"
      contains: "lifecycle: \"ended\""
    - path: "src/app/shell/AppShell.tsx"
      provides: "Conditional rendering of PromotionStatusLine"
      contains: "fallbackState"
    - path: "src/components/panel/PromotionStatusLine.test.tsx"
      provides: "Updated tests reflecting ended codex promotion and hidden fallback"
  key_links:
    - from: "src/features/promotions/catalog.ts"
      to: "resolvePromotionDisplayDecision"
      via: "promotion resolution reads catalog lifecycle"
      pattern: "lifecycle.*ended"
---

<objective>
End the Codex limited-time 2x promotion in the catalog and hide the PromotionStatusLine component entirely when no provider has active promotions (instead of showing "No promotion right now" fallback text).

Purpose: Keeps promotion catalog accurate and saves vertical header space when no promotions are active.
Output: Updated catalog, conditional rendering in AppShell, passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/features/promotions/catalog.ts
@src/app/shell/AppShell.tsx
@src/components/panel/PromotionStatusLine.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: End Codex promotion in catalog and hide PromotionStatusLine when empty</name>
  <files>src/features/promotions/catalog.ts, src/app/shell/AppShell.tsx</files>
  <action>
In `src/features/promotions/catalog.ts`:
- Change catalog-level `lastReviewedAt` (line 4) to `"2026-04-02T00:00:00Z"`.
- For the campaign with `id: "codex-limited-time-promotion"` (line 61):
  - Change `lifecycle` from `"active"` to `"ended"`.
  - Add `endsAt: "2026-04-02T00:00:00Z"` field (after `startsAt`).
  - Change `lastReviewedAt` to `"2026-04-02T00:00:00Z"`.
  - Update `historyNote` to `"Codex limited-time 2x promotion ended as confirmed by user on 2026-04-02."`.

In `src/app/shell/AppShell.tsx`:
- Around line 557 where `<PromotionStatusLine>` is rendered, wrap it in a condition:
  - Only render `<PromotionStatusLine>` when `!(promotionDecision.inlineServices.length === 0 && promotionDecision.fallbackState === "none")`.
  - When the condition is false (no active promotions, fallback is "none"), skip rendering entirely.
  - Use a simple pattern like:
    ```
    const shouldShowPromotionLine = promotionDecision.inlineServices.length > 0 || promotionDecision.fallbackState !== "none";
    ```
    Then wrap: `{shouldShowPromotionLine && <PromotionStatusLine ... />}`
  </action>
  <verify>
    <automated>npx vitest run src/components/panel/PromotionStatusLine.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>Codex promotion lifecycle is "ended" with endsAt and historyNote. PromotionStatusLine hidden when no active promotions.</done>
</task>

<task type="auto">
  <name>Task 2: Update PromotionStatusLine tests for ended Codex promotion</name>
  <files>src/components/panel/PromotionStatusLine.test.tsx</files>
  <action>
The test file's module-level `promotionDecision` fixture (lines 9-54) references the codex promotion as `status: "active-window"` with `matchedCampaignId: "codex-limited-time-promotion"`. Since the Codex promotion is now ended, the fixture should use the claude-code peak-hours restriction as the sole inline-visible service instead:

1. Update the module-level `promotionDecision` fixture:
   - Change `inlineServices` to contain a claude-code restriction entry (use the existing `allServices[1]` claude-code entry but with `isInlineVisible: true`).
   - Remove the codex entry from `inlineServices` (it would no longer be active).
   - Keep `allServices` with both entries but update codex to `status: "none"` since promotion ended.
   - Update `hiddenServiceCount` accordingly.
   - Update `fallbackState` to remain `null` (since claude-code restriction is still active/inline).

2. Update test assertions that reference codex pill elements:
   - Test "renders compact capsules..." (line 57): Change `promotion-pill-codex` references to `promotion-pill-claude-code`. Update text content assertions to match the restriction display (no "2x" benefit label for restrictions).
   - Test "renders richer all-state popover rows..." (line 74): Update to expect claude-code as the primary active entry. If codex appears, it should show "none" status.

3. Test "falls back to the unified no-promotion message..." (line 108): This test renders a `fallbackState: "none"` decision. Since the component is now hidden at AppShell level when fallbackState is "none", this test still validates the component's own rendering (PromotionStatusLine still renders the fallback internally when given that prop). Keep this test as-is — it tests the component in isolation, and the hiding logic is in AppShell, not in PromotionStatusLine itself.

4. Tests "routes hover/focus preview..." (line 142) and "still allows click-to-pin..." (line 174): Update the fixture references if they use the module-level `promotionDecision` with the old codex data.
  </action>
  <verify>
    <automated>npx vitest run src/components/panel/PromotionStatusLine.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>All PromotionStatusLine tests pass with updated fixtures reflecting ended Codex promotion. Test for fallback "none" state preserved (component-level test, hiding is AppShell's concern).</done>
</task>

</tasks>

<verification>
- `npx vitest run` passes all tests
- Catalog shows codex promotion as ended with correct metadata
- AppShell conditionally renders PromotionStatusLine
</verification>

<success_criteria>
1. Codex limited-time promotion marked as `lifecycle: "ended"` with `endsAt` and `historyNote`
2. PromotionStatusLine not rendered when `inlineServices` is empty and `fallbackState` is `"none"`
3. All existing tests updated and passing
</success_criteria>

<output>
After completion, create `.planning/quick/260402-f56-end-codex-promotion-and-hide-tips-for-pr/260402-f56-SUMMARY.md`
</output>

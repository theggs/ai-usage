---
phase: quick-260402-f56
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-04-02T03:18:31.992Z
---

<current_state>
Quick task to end the Codex promotion and hide PromotionStatusLine when empty. Core changes are done and committed. 15/15 promotion-specific tests pass. 3 AppShell integration tests need fixing for fake-timer/async interaction.
</current_state>

<completed_work>

- Task 1: End Codex promotion in catalog + hide PromotionStatusLine when empty — Done (commit ba23154)
  - `src/features/promotions/catalog.ts`: codex-limited-time-promotion lifecycle changed to "ended", added endsAt and historyNote
  - `src/app/shell/AppShell.tsx`: added `shouldShowPromotionLine` guard (line 469) — `promotionDecision.inlineServices.length > 0 || promotionDecision.fallbackState !== "none"`
  - `src/components/panel/PromotionStatusLine.test.tsx`: updated fixtures for ended codex (all 7 tests pass)
  - `src/features/promotions/resolver.test.ts`: updated to verify ended codex returns status "none" (all 6 tests pass)
- Phase 6 review: Cross-AI review completed (06-REVIEWS.md committed at cc008b2)
</completed_work>

<remaining_work>

- Fix 3 AppShell.test.tsx promotion integration tests:
  - Line 217: "renders promotion capsules and supports preview plus pinned popover"
  - Line 277: "resets the pinned promotion popover when the panel shell is reopened"
  - Line 601: "hides the whole main window when Escape is pressed"
  - Root cause: `vi.useFakeTimers()` prevents mock `loadProviderState` promises from resolving before assertions. The `shouldShowPromotionLine` evaluates to false because `mergedProviderStates` hasn't populated yet.
  - Fix: Use `await screen.findByTestId('promotion-pill-claude-code')` instead of `screen.getByTestId(...)`, or add `await waitFor(() => expect(screen.getByTestId(...)).toBeInTheDocument())` before the assertion block.
  - Note: 2 pace-danger tests also fail but are PRE-EXISTING failures, not caused by this change.
- Update STATE.md quick task table with completion record
- Write 260402-f56-SUMMARY.md
</remaining_work>

<decisions_made>

- Used `lifecycle: "ended"` (not just setting `endsAt`) — follows promotion-update-guide.md guidance
- Hide entire PromotionStatusLine when no active promotions — user explicitly requested hiding tips for providers without promotions
- Kept PromotionStatusLine component's internal fallback rendering intact — hiding logic is in AppShell, component still renders fallback if told to (separation of concerns)
</decisions_made>

<blockers>
- AppShell fake-timer test interaction: PromotionStatusLine doesn't render because async provider data loading hasn't completed when assertions run. Not blocking production code, only test assertions.
</blockers>

<context>
The Codex CLI still shows "Tip: New 2x rate limits until April 2nd." in its own output (visible in user's screenshot), but that's from the Codex CLI itself, not from our app. Our app now correctly hides the promotion line since no provider has active promotions (only claude-code peak-hours restriction remains active).

The worktree executor made changes against an older API version (used `loadPanelState`/`refreshPanelState` instead of `loadProviderState`/`refreshProviderState`), causing merge conflicts in test files. I restored the HEAD version of AppShell.test.tsx and manually applied promotion assertion changes, but the fake-timer timing issue remains.
</context>

<next_action>
Start with: Fix the 3 AppShell.test.tsx promotion tests by changing `screen.getByTestId('promotion-pill-claude-code')` to `await screen.findByTestId('promotion-pill-claude-code')` (or wrapping in `await waitFor()`). Then run `npx vitest run src/app/shell/AppShell.test.tsx` to verify. Then update STATE.md and write SUMMARY.md.
</next_action>

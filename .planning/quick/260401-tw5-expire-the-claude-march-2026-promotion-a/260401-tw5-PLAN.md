---
phase: quick
plan: 260401-tw5
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/promotions/catalog.ts
  - src/features/promotions/resolver.ts
  - src/features/promotions/types.ts
  - src/features/promotions/resolver.test.ts
  - src/components/panel/PromotionStatusLine.tsx
  - src/components/panel/PromotionStatusLine.test.tsx
  - src/app/shared/i18n.ts
  - src/app/shared/i18n.test.ts
autonomous: true
must_haves:
  truths:
    - "On and after 2026-04-01, the expired Claude March 2026 2x off-peak promotion no longer appears as an active Claude Code UI state."
    - "When Claude reports the newer peak-hours restriction window, Claude Code shows a distinct restriction state instead of a positive promotion state."
    - "The promotion trigger and popover stay compact, localized, and do not show a stale Claude 2x badge for the restriction path."
  artifacts:
    - path: "src/features/promotions/catalog.ts"
      provides: "Expired Claude March promotion lifecycle plus a new Claude peak-hours restriction campaign entry"
      contains: "claude-peak-hours"
    - path: "src/features/promotions/types.ts"
      provides: "Extended promotion model that can represent negative capacity events in the existing campaign system"
      contains: "restricted-window"
    - path: "src/features/promotions/resolver.ts"
      provides: "Resolver priority and detail timing for restriction windows"
      contains: "restricted-window"
    - path: "src/app/shared/i18n.ts"
      provides: "Localized copy for restriction status and timing"
      contains: "promotionStatusRestrictedWindow"
  key_links:
    - from: "src/features/promotions/catalog.ts"
      to: "src/features/promotions/resolver.ts"
      via: "campaign lifecycle and effect-aware status resolution"
      pattern: "capacityEffect|restricted-window|isCampaignCurrentlyActive"
    - from: "src/features/promotions/resolver.ts"
      to: "src/components/panel/PromotionStatusLine.tsx"
      via: "PromotionServiceDecision.status and benefitLabel"
      pattern: "status: \"restricted-window\"|benefitLabel"
    - from: "src/app/shared/i18n.ts"
      to: "src/components/panel/PromotionStatusLine.tsx"
      via: "restriction-specific compact/full labels and detail timing copy"
      pattern: "promotionStatusRestrictedWindow|promotionCompactStatusRestrictedWindow"
---

<objective>
Expire the stale Claude March 2026 2x off-peak promotion and extend the existing promotions pipeline to represent Claude peak-hours restriction as a negative capacity event.

Purpose: Keep Claude Code promotion status accurate as of 2026-04-01 without inventing a second status subsystem or redesigning the compact panel surface.
Output: One self-contained promotions-model update covering catalog, resolver, localized copy, and compact panel rendering/tests.
</objective>

<execution_context>
@/Users/chasewang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chasewang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/features/promotions/catalog.ts
@src/features/promotions/resolver.ts
@src/features/promotions/types.ts
@src/components/panel/PromotionStatusLine.tsx
@src/app/shared/i18n.ts

<interfaces>
From src/features/promotions/types.ts:
```ts
export type PromotionServiceStatus =
  | "active-window"
  | "active-general"
  | "inactive-window"
  | "eligibility-unknown"
  | "none";

export interface PromotionCampaign {
  id: string;
  serviceId: string;
  promotionType: PromotionType;
  benefitLabel?: string;
  startsAt: string;
  endsAt?: string;
  lifecycle: PromotionLifecycle;
  windows: PromotionWindow[];
}

export interface PromotionServiceDecision {
  serviceId: string;
  serviceName: string;
  status: PromotionServiceStatus;
  benefitLabel?: string;
  messageKey: string;
  detailTiming: PromotionDetailTiming;
  isInlineVisible: boolean;
}
```

From src/features/promotions/resolver.ts:
```ts
export const resolvePromotionDisplayDecision = ({
  now = new Date(),
  catalog = promotionCatalog,
  visibleServiceScope,
  panelStates,
  eligibilityByServiceId = derivePromotionEligibilityByService({ visibleServiceScope })
}: PromotionResolverInput): PromotionDisplayDecision => { ... };
```

From src/components/panel/PromotionStatusLine.tsx:
```ts
const STATUS_TONE_CLASS: Record<PromotionServiceDecision["status"], string> = { ... };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retire the March bonus and model Claude peak-hours restriction in the shared resolver</name>
  <files>src/features/promotions/catalog.ts, src/features/promotions/types.ts, src/features/promotions/resolver.ts, src/features/promotions/resolver.test.ts</files>
  <action>
Update the existing promotion campaign model instead of creating a parallel Claude-only path:

- In `src/features/promotions/catalog.ts`, move the currently active `claude-march-2026-usage-promotion` out of active circulation as of 2026-04-01 by changing its lifecycle to `ended` and refreshing the catalog review timestamp to the April 1 review date.
- Add a new Claude campaign entry for the newer peak-hours restriction signal in the same catalog. Keep it under `serviceId: "claude-code"` and `promotionType: "time-window"`, but represent it as a negative capacity event using a new model field such as `capacityEffect: "promotion" | "restriction"` (or an equivalently explicit name). Do not create a second resolver pipeline or a separate "restriction catalog".
- Express the restriction window with `activeRanges` for weekday ET peak hours so the resolver can treat it as active only inside the blocked period. The March bonus remains historical only.
- In `src/features/promotions/types.ts`, extend `PromotionCampaign`, `PromotionServiceStatus`, and `PromotionDetailTiming` so the resolver can emit a distinct Claude restriction state such as `"restricted-window"` and produce timing copy for an active local window, not only an "outside window" bonus case.
- In `src/features/promotions/resolver.ts`, make effect-aware decisions:
  - expired/ended campaigns never surface in UI decisions, even if their date range would otherwise match;
  - an active restriction window resolves to the new restriction status and outranks any positive status if both ever overlap;
  - negative events do not fabricate a `benefitLabel`;
  - outside the restriction window, Claude should fall back to `none` unless another active campaign is actually applicable.
- Update `src/features/promotions/resolver.test.ts` to lock the new contract:
  - the default catalog does not show the March 2026 Claude 2x promotion as active on 2026-04-01;
  - Claude resolves to the new restriction status during the ET peak-hours window;
  - Claude resolves to `none` outside that restriction window when no positive campaign is active;
  - Codex continuous promotion behavior stays unchanged.
  </action>
  <verify>
    <automated>cd /Users/chasewang/01workspace/projects/ai-usage && npx vitest run src/features/promotions/resolver.test.ts</automated>
  </verify>
  <done>The catalog no longer exposes the expired March Claude promotion as active, the new negative event is represented inside the shared campaign model, and resolver tests prove the restriction-vs-none behavior on April 1, 2026.</done>
</task>

<task type="auto">
  <name>Task 2: Localize and render the restriction state in the compact promotion surface</name>
  <files>src/components/panel/PromotionStatusLine.tsx, src/components/panel/PromotionStatusLine.test.tsx, src/app/shared/i18n.ts, src/app/shared/i18n.test.ts</files>
  <action>
Consume the new resolver state without redesigning the panel:

- In `src/app/shared/i18n.ts`, add English and Simplified Chinese copy for the new restriction status and its timing detail. Keep the text compact and operational, for example a distinct full label like "peak-hours restriction active" / "高峰时段受限" and a compact label that fits the existing capsule. Add a separate local-window detail template for "during weekdays {range}" instead of reusing the current "outside weekdays {range}" bonus copy.
- In `src/components/panel/PromotionStatusLine.tsx`, extend status-tone/status-label handling for the new status while preserving the current layout. Reuse the existing inactive/neutral visual tone if that avoids extra CSS churn; do not widen the popover or redesign the trigger row. Only render the benefit badge when a decision actually carries a positive label, so the Claude restriction row does not show a stale `2x`.
- Update `src/components/panel/PromotionStatusLine.test.tsx` so the fixture uses the new Claude restriction state and asserts:
  - the popover shows restriction-specific status text;
  - the Claude row no longer renders a `2x` benefit badge for the restriction path;
  - the compact trigger remains the same surface and click/hover behavior still works.
- Update `src/app/shared/i18n.test.ts` to cover the new localized status/detail strings and ensure promotion summary formatting reflects a restriction message instead of the expired March off-peak bonus.
  </action>
  <verify>
    <automated>cd /Users/chasewang/01workspace/projects/ai-usage && npx vitest run src/components/panel/PromotionStatusLine.test.tsx src/app/shared/i18n.test.ts</automated>
  </verify>
  <done>The compact promotion UI can display Claude restriction state with localized copy, no stale 2x badge is shown for that negative event, and the interaction footprint remains unchanged.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/features/promotions/resolver.test.ts src/components/panel/PromotionStatusLine.test.tsx src/app/shared/i18n.test.ts` passes
- `rg -n "claude-march-2026-usage-promotion|claude-peak-hours|restricted-window" src/features/promotions` shows the expired Claude March entry plus the new restriction-aware path
- `rg -n "promotionStatusRestrictedWindow|promotionCompactStatusRestrictedWindow" src/app/shared/i18n.ts src/components/panel/PromotionStatusLine.tsx` shows the new localized UI path
</verification>

<success_criteria>
1. As of 2026-04-01, the Claude March 2026 2x bonus no longer resolves as an active Claude Code UI state.
2. Claude peak-hours restriction is modeled and rendered as a distinct negative event inside the existing promotions system.
3. The panel stays compact and localized, and the Claude restriction state does not display a misleading positive benefit badge.
</success_criteria>

<output>
After completion, create `.planning/quick/260401-tw5-expire-the-claude-march-2026-promotion-a/260401-tw5-SUMMARY.md`
</output>

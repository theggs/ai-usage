---
phase: quick
plan: 260401-uo3
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/tauri/summary.ts
  - src/components/panel/ServiceCard.tsx
  - src/components/panel/ServiceCard.test.tsx
autonomous: true
must_haves:
  truths:
    - "Kimi Code and GLM quota cards display shorter windows before weekly windows, matching Codex and Claude Code."
    - "The fix is display-only and does not alter provider ordering or quota values."
    - "A regression test covers reversed backend quota order."
  artifacts:
    - path: "src/lib/tauri/summary.ts"
      provides: "Shared quota-dimension display sort helper"
      contains: "sortQuotaDimensionsForDisplay"
    - path: "src/components/panel/ServiceCard.tsx"
      provides: "Service cards render quota rows in normalized display order"
      contains: "sortedDimensions"
    - path: "src/components/panel/ServiceCard.test.tsx"
      provides: "Regression coverage for reversed Kimi quota order"
      contains: "orders shorter quota windows before weekly windows"
---

<objective>
Normalize panel quota row ordering so service cards consistently render shorter windows before longer windows.

Purpose: Remove the visual inconsistency where Kimi Code and GLM can show weekly quota ahead of 5h quota when backend data arrives reversed.
Output: One shared display-order helper, wired into service-card rendering, with regression coverage.
</objective>

<verification>
- `npx vitest run src/components/panel/ServiceCard.test.tsx`
- `npx vitest run src/lib/tauri/summary.test.ts src/app/panel/PanelView.test.tsx`
</verification>

<success_criteria>
1. Service cards show `5h` before `week` regardless of backend quota order.
2. Existing panel summary behavior remains green in nearby tests.
3. The regression is covered by a focused component test.
</success_criteria>

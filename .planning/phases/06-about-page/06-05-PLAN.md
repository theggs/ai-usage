---
phase: 06-about-page
plan: 05
type: execute
wave: 4
depends_on:
  - "06-04"
files_modified:
  - src/app/shared/i18n.ts
  - src/app/shared/i18n.test.ts
  - src/app/settings/SettingsView.tsx
  - src/app/settings/SettingsView.test.tsx
autonomous: true
gap_closure: true
requirements:
  - ABOUT-01
must_haves:
  truths:
    - "The Settings footer entry opens About through the existing shared navigation path and reads naturally in zh-CN"
    - "The footer disclosure chevron is rendered as UI chrome, not baked into localized copy"
    - "The Phase 06 About behavior already accepted in UAT stays unchanged outside the footer label treatment"
  artifacts:
    - path: src/app/shared/i18n.ts
      provides: label-only localized About footer copy
      contains: 'aboutLink: "About"'
    - path: src/app/settings/SettingsView.tsx
      provides: footer button that composes label text with separate chevron chrome
      contains: "aria-hidden"
    - path: src/app/settings/SettingsView.test.tsx
      provides: regression coverage for footer rendering and navigation callback
      contains: "openAbout"
  key_links:
    - from: src/app/settings/SettingsView.tsx
      to: src/app/shared/appState.ts
      via: useAppState().openAbout
      pattern: "openAbout"
    - from: src/app/settings/SettingsView.tsx
      to: src/app/shared/i18n.ts
      via: copy.aboutLink plus separate chevron element
      pattern: "copy\\.aboutLink"
---

<objective>
Close the diagnosed Phase 06 cosmetic UAT gap by cleaning up the Settings footer About entry without changing the accepted About navigation flow.

Purpose: UAT confirmed the Settings-to-About behavior is correct, but the footer entry `关于 AIUsage >` is awkward because wording, product name, and chevron are collapsed into one localized string. This plan keeps D-01's Settings-only entry point and D-09's centralized localization, while splitting label copy from disclosure chrome.
Output: label-only localized footer copy in `i18n.ts`, a composed footer button in `SettingsView.tsx`, and regression tests covering both the copy contract and `openAbout()` wiring.
</objective>

<execution_context>
@/Users/chasewang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chasewang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-about-page/06-CONTEXT.md
@.planning/phases/06-about-page/06-UAT.md
@.planning/phases/06-about-page/06-04-PLAN.md
@.planning/phases/06-about-page/06-04-SUMMARY.md
@.planning/debug/settings-about-entry-copy.md
@src/app/shared/i18n.ts
@src/app/settings/SettingsView.tsx
@src/app/settings/SettingsView.test.tsx

## Locked decisions to preserve
- D-01: About is reached from a Settings footer link only.
- D-09: Visible labels stay localized through `i18n.ts`.

<interfaces>
From src/app/shared/appState.ts:
```typescript
export interface AppStateValue {
  currentView: "panel" | "settings" | "about";
  openAbout: () => void;
  closeAbout: () => void;
}
```

From src/app/shared/i18n.ts (current footer copy contract):
```typescript
export type CopyTree = {
  aboutLink: string;
};

aboutLink: "About AIUsage >"
// zh-CN:
aboutLink: "关于 AIUsage >"
```

From src/app/settings/SettingsView.tsx (current footer rendering):
```tsx
<button onClick={openAbout} type="button">
  {copy.aboutLink}
</button>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Narrow the About footer copy contract to a label-only localized string</name>
  <files>src/app/shared/i18n.ts, src/app/shared/i18n.test.ts</files>
  <action>
    Preserve D-09 by keeping the footer label on the shared copy tree, but remove presentation chrome from the translation content. Change `aboutLink` to label-only copy: use `About` in `baseCopy` and `关于` in `localeCopy["zh-CN"]`. Do not add the chevron or `AIUsage` into the translation string, and do not introduce a second hardcoded label path in `SettingsView.tsx`.

    Add or update i18n regression coverage so both locales assert the cleaned `aboutLink` values and explicitly guard against a baked-in `>` disclosure character.
  </action>
  <verify>
    <automated>npx vitest run src/app/shared/i18n.test.ts</automated>
  </verify>
  <done>
    `getCopy("en-US").aboutLink` returns `About`, `getCopy("zh-CN").aboutLink` returns `关于`, and the locale tests prove the footer copy no longer embeds the chevron.
  </done>
</task>

<task type="auto">
  <name>Task 2: Render the footer disclosure chrome separately and keep openAbout() intact</name>
  <files>src/app/settings/SettingsView.tsx, src/app/settings/SettingsView.test.tsx</files>
  <action>
    Preserve D-01 and the accepted 06-04 navigation behavior. Keep the footer in the same location, keep `openAbout()` as the click handler, and keep the button centered after the settings surfaces. Replace the raw `{copy.aboutLink}` rendering with a composed inline layout that renders the localized label plus a dedicated chevron element as visual chrome. Mark the chevron `aria-hidden="true"` so the accessible button name stays the localized label only.

    Add a focused component regression test in `SettingsView.test.tsx` that proves:
    1. the footer button is discoverable by its localized label,
    2. clicking it calls `openAbout`,
    3. the chevron is rendered as a separate `aria-hidden` element rather than being part of the localized string.

    Do not change the Settings footer placement, About routing contract, or any About page content.
  </action>
  <verify>
    <automated>npx vitest run src/app/settings/SettingsView.test.tsx src/app/shared/i18n.test.ts</automated>
  </verify>
  <done>
    The Settings footer still opens About through `openAbout()`, the button's accessible name is the cleaned localized label, and the chevron is rendered as separate UI chrome.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/app/shared/i18n.test.ts src/app/settings/SettingsView.test.tsx` passes.
- `rg -n 'aboutLink: "About"|aboutLink: "关于"' src/app/shared/i18n.ts` confirms label-only copy.
- `rg -n 'aria-hidden="true"|copy\\.aboutLink|openAbout' src/app/settings/SettingsView.tsx` confirms the footer composes copy and chrome while preserving shared navigation.
</verification>

<success_criteria>
- The Settings footer entry no longer shows `关于 AIUsage >`.
- zh-CN uses a natural localized label while the chevron remains visible as styling chrome.
- The already-accepted Settings -> About -> Settings behavior remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/06-about-page/06-05-SUMMARY.md`
</output>

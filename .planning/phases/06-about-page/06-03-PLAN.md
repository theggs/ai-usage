---
phase: 06-about-page
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
read_first:
  - .planning/phases/06-about-page/06-CONTEXT.md
  - .planning/phases/06-about-page/06-REVIEWS.md
  - .planning/phases/06-about-page/06-UI-SPEC.md
  - src/app/about/AboutView.tsx
  - src/app/shared/i18n.ts
  - src/app/shell/AppShell.tsx
  - src/generated/license-audit.json
files_modified:
  - src/app/about/AboutView.tsx
  - src/app/shell/AppShell.tsx
  - src/assets/icons/app-icon.png
  - src/app/shared/i18n.ts
autonomous: true
requirements:
  - ABOUT-01
  - ABOUT-02
  - ABOUT-03
  - ABOUT-04
  - ABOUT-05
  - ABOUT-06
must_haves:
  truths:
    - About page shows app icon, app name, and version sourced from Tauri metadata without rendering a bare fallback `v`
    - key-value rows show app license, GitHub link, and dependency audit summary from the generated JSON
    - dependency audit text distinguishes copyleft risk from unknown-license coverage, and any badges are localized
    - the About layout remains an extensible key-value list and only mounts when the user enters the About view
  artifacts:
    - path: src/app/about/AboutView.tsx
      provides: About page component with hero section, key-value rows, and audit summary rendering
    - path: src/app/shell/AppShell.tsx
      provides: Replacement of the placeholder slot with a guarded AboutView mount
    - path: src/assets/icons/app-icon.png
      provides: Frontend-importable app icon asset for the About hero
    - path: src/app/shared/i18n.ts
      provides: Localized About labels plus localized copyleft/unknown-license summary and badge copy
  key_links:
    - from: src/app/about/AboutView.tsx
      to: src/generated/license-audit.json
      via: static JSON import
      pattern: 'unknownLicenseCount|copyleftCount'
    - from: src/app/about/AboutView.tsx
      to: @tauri-apps/api/app
      via: getVersion()
      pattern: 'getVersion'
    - from: src/app/about/AboutView.tsx
      to: @tauri-apps/plugin-shell
      via: open()
      pattern: 'plugin-shell'
    - from: src/app/shell/AppShell.tsx
      to: src/app/about/AboutView.tsx
      via: currentView guarded render
      pattern: 'currentView === "about" \\? <AboutView /> : null'
---

<objective>
Implement the actual About page UI and replace the shell placeholder with the real component, while tightening version loading, localization, and audit-risk presentation in response to the review.

Purpose: This is the user-facing delivery for ABOUT-02 through ABOUT-06, and it closes the medium/low review issues around version flicker, eager rendering, hardcoded fallback version, and unlocalized badges.
Output: A production AboutView, localized About copy, the imported app icon, and the final AppShell wiring to mount AboutView only when active.
</objective>

<execution_context>
@/Users/chasewang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chasewang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-about-page/06-CONTEXT.md
@.planning/phases/06-about-page/06-REVIEWS.md
@.planning/phases/06-about-page/06-UI-SPEC.md

## Locked decisions to implement
- D-06: Hero uses the existing app icon asset plus app name and version.
- D-07: The information block must stay a key-value list so future rows can be added without layout changes.
- D-08: GitHub row opens in the default browser via Tauri shell.
- D-09: About labels are localized through `i18n.ts`.
- D-10: Version comes from Tauri package metadata.
- D-11: App license is Apache 2.0.
- D-12: The dependency row shows summary data, not a full package list.

## Review concerns to address
- MEDIUM: do not render a bare `v` before `getVersion()` resolves.
- LOW: do not eagerly mount AboutView while it is hidden.
- LOW: remove the hardcoded `"0.1.0"` fallback.
- LOW: localize copyleft/unknown-license badges instead of hardcoding English.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the About hero asset for a static frontend import</name>
  <files>src/assets/icons/app-icon.png</files>
  <action>Copy `src-tauri/icons/128x128.png` into `src/assets/icons/app-icon.png` so the About hero can import a stable frontend asset without reading from the Tauri bundle at runtime. Keep the filename stable because `AboutView.tsx` will import it directly.</action>
  <verify>
    <automated>test -f src/assets/icons/app-icon.png</automated>
  </verify>
  <done>`src/assets/icons/app-icon.png` exists and is ready for static import in the About hero.</done>
</task>

<task type="auto">
  <name>Task 2: Localize About labels, summary strings, and audit-risk badges</name>
  <files>src/app/shared/i18n.ts</files>
  <action>Extend `CopyTree`, `baseCopy`, and `localeCopy["zh-CN"]` with the complete About copy set needed by the final component. Keep the existing keys for title/license/GitHub/dependencies, keep `aboutLink` in place for Plan 04, and add localized strings for the review-driven states: `aboutVersionUnavailable`, `aboutDepsUnknownFound`, `aboutDepsMixedRisk`, `aboutCopyleftBadge`, and `aboutUnknownBadge`. The summary copy must let the UI distinguish: all reviewed licenses permissive, copyleft found, unknown licenses found, and the mixed case where both appear. Do not hardcode English words like `copyleft` or `unknown` inside the component; all visible strings must come from `i18n.ts` per D-09.</action>
  <verify>
    <automated>rg -n 'aboutVersionUnavailable|aboutDepsUnknownFound|aboutDepsMixedRisk|aboutCopyleftBadge|aboutUnknownBadge' src/app/shared/i18n.ts</automated>
  </verify>
  <done>`src/app/shared/i18n.ts` contains localized About summary/badge copy for both locales, including unknown-license coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Implement AboutView and swap the shell placeholder for a guarded About mount</name>
  <files>src/app/about/AboutView.tsx, src/app/shell/AppShell.tsx</files>
  <action>Build the final About page and wire it into the shell. In `src/app/about/AboutView.tsx`, render the hero and key-value list from the UI spec, import `src/generated/license-audit.json`, and derive the dependency summary from `copyleftCount` and `unknownLicenseCount` without ever describing unknowns as permissive. Use `const [version, setVersion] = useState<string | null>(null)` (or `undefined`) so the component never renders a bare `v`; either hide the version line until `getVersion()` resolves or show the localized `copy.aboutVersionUnavailable` if the call fails. Keep the GitHub row wired through `@tauri-apps/plugin-shell.open`, keep the app license row static as Apache 2.0 per D-11, and preserve the extensible key-value structure from D-07. In `src/app/shell/AppShell.tsx`, replace the About placeholder with `currentView === "about" ? <AboutView /> : null` and add the import there so the component mounts only when visible. This task explicitly addresses the version flicker, eager-render, hardcoded fallback, and unlocalized-badge review findings.</action>
  <verify>
    <automated>npx tsc --noEmit && rg -n 'useState<string \\| null>|getVersion|unknownLicenseCount|aboutCopyleftBadge|aboutUnknownBadge' src/app/about/AboutView.tsx && rg -n 'currentView === \"about\" \\? <AboutView /> : null' src/app/shell/AppShell.tsx</automated>
  </verify>
  <done>`AboutView` renders real metadata and audit risk honestly, and `AppShell` mounts it only when the user actually opens About.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with the new About component and shell wiring.
- `rg -n 'unknownLicenseCount' src/app/about/AboutView.tsx` proves the UI surfaces audit coverage, not just copyleft counts.
- `rg -n 'aboutCopyleftBadge|aboutUnknownBadge' src/app/shared/i18n.ts` proves badges are localized.
</verification>

<success_criteria>
- The About hero shows a real Tauri version when available and never flashes a bare `v`.
- The dependency row can represent copyleft findings, unknown-license findings, and mixed risk without hardcoded English.
- The About page remains a key-value list that can absorb future rows without structural rewrites.
- Hidden About content does not mount until `currentView` changes to `about`.
</success_criteria>

<output>
After completion, create `.planning/phases/06-about-page/06-03-SUMMARY.md`
</output>

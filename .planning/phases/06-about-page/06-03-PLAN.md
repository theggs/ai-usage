---
phase: 06-about-page
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
read_first:
  - .planning/phases/06-about-page/06-CONTEXT.md
  - .planning/phases/06-about-page/06-REVIEWS.md
  - .planning/phases/06-about-page/06-UI-SPEC.md
  - src/app/about/AboutView.tsx
  - src-tauri/tauri.conf.json
  - LICENSE
  - src/app/shared/i18n.ts
  - src/app/shell/AppShell.tsx
  - src/generated/license-audit.json
files_modified:
  - LICENSE
  - src/app/about/AboutView.tsx
  - src/app/shell/AppShell.tsx
  - src/assets/icons/app-icon.png
  - src/app/shared/i18n.ts
autonomous: true
requirements:
  - ABOUT-01
  - ABOUT-02
  - ABOUT-03
  - ABOUT-04
  - ABOUT-05
  - ABOUT-06
must_haves:
  truths:
    - About page shows app icon, app name, version, and build info sourced from Tauri package metadata without rendering a bare fallback `v`
    - key-value rows show the app license, GitHub link, build info, and dependency audit summary from project artifacts
    - dependency audit text distinguishes copyleft risk from unknown-license coverage, and any badges are localized
    - the About layout remains an extensible key-value list and only mounts when the user enters the About view
  artifacts:
    - path: LICENSE
      provides: Project-root Apache 2.0 license text required by D-11 and consumed as the source of truth for ABOUT-04
    - path: src/app/about/AboutView.tsx
      provides: About page component with hero section, version/build metadata, canonical GitHub link, key-value rows, and audit summary rendering
    - path: src/app/shell/AppShell.tsx
      provides: Replacement of the placeholder slot with a guarded AboutView mount
    - path: src/assets/icons/app-icon.png
      provides: Frontend-importable app icon asset for the About hero
    - path: src/app/shared/i18n.ts
      provides: Localized About labels plus build-info and copyleft/unknown-license summary and badge copy
  key_links:
    - from: src/app/about/AboutView.tsx
      to: src/generated/license-audit.json
      via: static JSON import
      pattern: 'unknownLicenseCount|copyleftCount'
    - from: src/app/about/AboutView.tsx
      to: @tauri-apps/api/app
      via: getVersion()
      pattern: 'getVersion'
    - from: src/app/about/AboutView.tsx
      to: src-tauri/tauri.conf.json
      via: static metadata import for productName and identifier build info
      pattern: 'productName|identifier'
    - from: src/app/about/AboutView.tsx
      to: @tauri-apps/plugin-shell
      via: open()
      pattern: 'https://github\\.com/theggs/ai-usage|plugin-shell|open\\(GITHUB_URL\\)'
    - from: src/app/shell/AppShell.tsx
      to: src/app/about/AboutView.tsx
      via: currentView guarded render
      pattern: 'currentView === "about" \\? <AboutView /> : null'
---

<objective>
Implement the actual About page UI and replace the shell placeholder with the real component, while tightening version/build metadata, license-file coverage, localization, and audit-risk presentation in response to the review.

Purpose: This is the user-facing delivery for ABOUT-02 through ABOUT-06, and it closes the blocker around missing build-info coverage plus the locked D-11 requirement for a root Apache 2.0 license file.
Output: A production AboutView, verified root `LICENSE` file, localized About copy, the imported app icon, and the final AppShell wiring to mount AboutView only when active.
</objective>

<execution_context>
@/Users/chasewang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chasewang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-about-page/06-CONTEXT.md
@.planning/phases/06-about-page/06-REVIEWS.md
@.planning/phases/06-about-page/06-UI-SPEC.md

## Locked decisions to implement
- D-06: Hero uses the existing app icon asset plus app name and version.
- D-07: The information block must stay a key-value list so future rows can be added without layout changes.
- D-08: GitHub row opens in the default browser via Tauri shell.
- D-09: About labels are localized through `i18n.ts`.
- D-10: Version comes from Tauri package metadata.
- D-11: App license is Apache 2.0 and the project root must contain a `LICENSE` file with that text.
- D-12: The dependency row shows summary data, not a full package list.

## Review concerns to address
- MEDIUM: do not render a bare `v` before `getVersion()` resolves.
- LOW: do not eagerly mount AboutView while it is hidden.
- LOW: remove the hardcoded `"0.1.0"` fallback.
- LOW: localize copyleft/unknown-license badges instead of hardcoding English.

## Canonical GitHub URL for ABOUT-03
- Source: `git remote get-url origin` resolves to `git@github.com:theggs/ai-usage.git`.
- Required About URL value: `https://github.com/theggs/ai-usage`
- Implementation rule: use that exact HTTPS URL for both the rendered row value and the `shell.open(...)` target; replace any stale `anomalyco/ai-usage` constant if present.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the About hero asset for a static frontend import</name>
  <files>src/assets/icons/app-icon.png</files>
  <action>Copy `src-tauri/icons/128x128.png` into `src/assets/icons/app-icon.png` so the About hero can import a stable frontend asset without reading from the Tauri bundle at runtime. Keep the filename stable because `AboutView.tsx` will import it directly.</action>
  <verify>
    <automated>test -f src/assets/icons/app-icon.png</automated>
  </verify>
  <done>`src/assets/icons/app-icon.png` exists and is ready for static import in the About hero.</done>
</task>

<task type="auto">
  <name>Task 2: Localize About labels, summary strings, and audit-risk badges</name>
  <files>src/app/shared/i18n.ts</files>
  <action>Extend `CopyTree`, `baseCopy`, and `localeCopy["zh-CN"]` with the complete About copy set needed by the final component. Keep the existing keys for title/license/GitHub/dependencies, keep `aboutLink` in place for Plan 04, and add localized strings for the checker-driven states: `aboutBuildInfoLabel`, `aboutVersionUnavailable`, `aboutDepsUnknownFound`, `aboutDepsMixedRisk`, `aboutCopyleftBadge`, and `aboutUnknownBadge`. The summary copy must let the UI distinguish: all reviewed licenses permissive, copyleft found, unknown licenses found, and the mixed case where both appear. Do not hardcode English words like `copyleft`, `unknown`, or `Build` inside the component; all visible strings must come from `i18n.ts` per D-09.</action>
  <verify>
    <automated>rg -n 'aboutBuildInfoLabel|aboutVersionUnavailable|aboutDepsUnknownFound|aboutDepsMixedRisk|aboutCopyleftBadge|aboutUnknownBadge' src/app/shared/i18n.ts</automated>
  </verify>
  <done>`src/app/shared/i18n.ts` contains localized About build-info and summary/badge copy for both locales, including unknown-license coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Implement AboutView and swap the shell placeholder for a guarded About mount</name>
  <files>LICENSE, src/app/about/AboutView.tsx, src/app/shell/AppShell.tsx</files>
  <action>Build the final About page and wire it into the shell. First, verify the project-root `LICENSE` file contains Apache 2.0 text; if it is missing or incorrect, create/update it so D-11 and ABOUT-04 are satisfied from a real project artifact. In `src/app/about/AboutView.tsx`, render the hero and key-value list from the UI spec, import `src/generated/license-audit.json`, and derive the dependency summary from `copyleftCount` and `unknownLicenseCount` without ever describing unknowns as permissive. Add explicit ABOUT-02 coverage by sourcing build info from `src-tauri/tauri.conf.json` and rendering a localized build-info row that shows the Tauri metadata needed for user-visible build identification, at minimum `productName` and `identifier`, while `getVersion()` continues to supply the version string. Add explicit ABOUT-03 coverage by setting `const GITHUB_URL = "https://github.com/theggs/ai-usage"` based on the canonical `origin` remote and render that same URL text in the GitHub row; `handleOpenGitHub` must pass that exact constant to `@tauri-apps/plugin-shell.open`. Use `const [version, setVersion] = useState<string | null>(null)` (or `undefined`) so the component never renders a bare `v`; either hide the version line until `getVersion()` resolves or show the localized `copy.aboutVersionUnavailable` if the call fails. Keep the app license row tied to the verified Apache 2.0 project license, and preserve the extensible key-value structure from D-07. In `src/app/shell/AppShell.tsx`, replace the About placeholder with `currentView === "about" ? <AboutView /> : null` and add the import there so the component mounts only when visible. This task explicitly addresses the build-info blocker, ABOUT-03 URL ambiguity, D-11, version flicker, eager-render, hardcoded fallback, and unlocalized-badge findings.</action>
  <verify>
    <automated>npx tsc --noEmit && rg -n 'useState<string \\| null>|getVersion|productName|identifier|aboutBuildInfoLabel|unknownLicenseCount|aboutCopyleftBadge|aboutUnknownBadge|const GITHUB_URL = \"https://github\\.com/theggs/ai-usage\"|open\\(GITHUB_URL\\)|github\\.com/theggs/ai-usage' src/app/about/AboutView.tsx && rg -n 'currentView === \"about\" \\? <AboutView /> : null' src/app/shell/AppShell.tsx && rg -n 'Apache License|Version 2.0' LICENSE</automated>
  </verify>
  <done>`AboutView` renders version plus explicit build info from Tauri metadata, uses the exact canonical GitHub URL `https://github.com/theggs/ai-usage` for both display and `shell.open`, the root `LICENSE` artifact is present with Apache 2.0 text, and `AppShell` mounts About only when the user opens it.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with the new About component and shell wiring.
- `rg -n 'productName|identifier' src/app/about/AboutView.tsx` proves ABOUT-02 includes explicit build-info sourcing rather than version only.
- `rg -n 'const GITHUB_URL = "https://github.com/theggs/ai-usage"|open\\(GITHUB_URL\\)|github.com/theggs/ai-usage' src/app/about/AboutView.tsx` proves ABOUT-03 uses one pinned canonical URL for both rendering and `shell.open`.
- `rg -n 'Apache License|Version 2.0' LICENSE` proves the root app license artifact required by D-11 is covered by the plan.
- `rg -n 'unknownLicenseCount' src/app/about/AboutView.tsx` proves the UI surfaces audit coverage, not just copyleft counts.
- `rg -n 'aboutCopyleftBadge|aboutUnknownBadge' src/app/shared/i18n.ts` proves badges are localized.
</verification>

<success_criteria>
- The About hero shows a real Tauri version when available and never flashes a bare `v`.
- The About page also renders build info sourced from `src-tauri/tauri.conf.json`, not just the version number.
- The GitHub row renders the exact canonical URL `https://github.com/theggs/ai-usage` and opens that same URL in the default browser.
- The project root contains Apache 2.0 license text and the About page points to that app-license reality.
- The dependency row can represent copyleft findings, unknown-license findings, and mixed risk without hardcoded English.
- The About page remains a key-value list that can absorb future rows without structural rewrites.
- Hidden About content does not mount until `currentView` changes to `about`.
</success_criteria>

<output>
After completion, create `.planning/phases/06-about-page/06-03-SUMMARY.md`
</output>

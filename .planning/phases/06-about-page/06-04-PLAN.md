---
phase: 06-about-page
plan: 04
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-03"
read_first:
  - .planning/phases/06-about-page/06-CONTEXT.md
  - .planning/phases/06-about-page/06-UI-SPEC.md
  - src/app/settings/SettingsView.tsx
  - src/app/shared/i18n.ts
  - src/app/shared/appState.ts
files_modified:
  - src/app/settings/SettingsView.tsx
  - src/app/shared/i18n.ts
autonomous: true
requirements:
  - ABOUT-01
must_haves:
  truths:
    - Settings has a localized footer link that opens the About page
    - the footer link uses the shared app-state navigation path instead of duplicating shell logic locally
    - the link sits after the settings cards and before overlay/status UI, matching the approved access pattern
  artifacts:
    - path: src/app/settings/SettingsView.tsx
      provides: About footer button that calls openAbout()
    - path: src/app/shared/i18n.ts
      provides: localized aboutLink string consumed by SettingsView
  key_links:
    - from: src/app/settings/SettingsView.tsx
      to: src/app/shared/appState.ts
      via: useAppState().openAbout
      pattern: 'openAbout'
    - from: src/app/settings/SettingsView.tsx
      to: src/app/shared/i18n.ts
      via: copy.aboutLink
      pattern: 'aboutLink'
---

<objective>
Add the only user-facing entry point for About: a localized footer link in Settings that routes through the shared shell navigation contract.

Purpose: D-01 requires About to be reachable from the Settings footer and nowhere else.
Output: A localized footer button in `SettingsView.tsx` plus the `aboutLink` copy key in `i18n.ts`.
</objective>

<execution_context>
@/Users/chasewang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chasewang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-about-page/06-CONTEXT.md
@.planning/phases/06-about-page/06-UI-SPEC.md

## Locked decisions to implement
- D-01: About is reached from a Settings footer link only.
- D-09: Visible labels are localized through `i18n.ts`.

## Existing integration points
- `src/app/settings/SettingsView.tsx` already consumes `useAppState()` and `getCopy()`.
- Plan 01 adds `openAbout()` to the app-state contract.
- Plan 03 keeps `aboutLink` out of its copy changes so this plan owns the Settings footer string end-to-end.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the localized Settings footer label for About</name>
  <files>src/app/shared/i18n.ts</files>
  <action>Ensure `CopyTree`, `baseCopy`, and `localeCopy["zh-CN"]` include `aboutLink`. Use `About AIUsage >` for the English baseline and `关于 AIUsage >` for zh-CN so the footer can reuse the existing copy pipeline rather than embedding literal text in `SettingsView.tsx`.</action>
  <verify>
    <automated>rg -n 'aboutLink: \"About AIUsage >\"|aboutLink: \"关于 AIUsage >\"' src/app/shared/i18n.ts</automated>
  </verify>
  <done>`src/app/shared/i18n.ts` contains the localized `aboutLink` entry in both locales.</done>
</task>

<task type="auto">
  <name>Task 2: Add the About footer button in Settings and route it through openAbout()</name>
  <files>src/app/settings/SettingsView.tsx</files>
  <action>Destructure `openAbout` from `useAppState()` and add the footer button after the final settings card but before the drag overlay portal / status messages described in the UI spec. Render `copy.aboutLink`, keep the visual treatment `text-[13px] text-slate-400 transition-colors hover:text-slate-600`, and call `openAbout` directly on click. Do not introduce a second navigation state in Settings; the footer must use the shared shell path from Plan 01.</action>
  <verify>
    <automated>rg -n 'openAbout' src/app/settings/SettingsView.tsx && rg -n 'copy\\.aboutLink' src/app/settings/SettingsView.tsx && rg -n 'mt-4 flex justify-center' src/app/settings/SettingsView.tsx</automated>
  </verify>
  <done>`SettingsView.tsx` contains a localized footer button in the approved location and it routes through `openAbout()`.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with the Settings footer wired to `openAbout`.
- `rg -n 'copy.aboutLink' src/app/settings/SettingsView.tsx` confirms the footer text is localized instead of hardcoded.
</verification>

<success_criteria>
- Settings exposes a single About entry point in the footer.
- The footer text is localized in both supported languages.
- Clicking the footer uses the app-state shell navigation contract rather than duplicating view logic inside Settings.
</success_criteria>

<output>
After completion, create `.planning/phases/06-about-page/06-04-SUMMARY.md`
</output>

---
phase: 06-about-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/shared/appState.ts
  - src/app/shell/AppShell.tsx
autonomous: true
requirements:
  - ABOUT-01
must_haves:
  truths:
    - currentView type accepts "about" as a valid view state
    - AppShell renders a 3-view slide container (300% width)
    - About view can be navigated to from Settings and back
  artifacts:
    - path: src/app/shared/appState.ts
      provides: Extended AppStateValue with "about" in currentView type and openAbout method
    - path: src/app/shell/AppShell.tsx
      provides: 3-view slide animation container and About view slot
  key_links:
    - from: appState.ts currentView type
      to: AppShell.tsx currentView state usage
      via: TypeScript type checking
    - from: AppShell.tsx openAbout method
      to: AppStateContext.Provider value
      via: openAbout function in context
---

<objective>
Extend the view system from 2 views (panel/settings) to 3 views (panel/settings/about) to support the new About page as an independent view with slide animation.

Purpose: ABOUT-01 requires the About page to be an independent page (not a Settings tab). This plan extends the existing view infrastructure to support a third view with the same slide animation pattern.
Output: Extended appState.ts with "about" in currentView type, AppShell.tsx with 3-view slide container (w-[300%]) and About view slot.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-about-page/06-CONTEXT.md
@.planning/phases/06-about-page/06-UI-SPEC.md

## Key Implementation Details from CONTEXT.md
- D-02: About page slides in from the right using same CSS translate animation as panel-to-settings
- D-03: Back arrow in About header returns to Settings (not Panel)
- D-05: On window blur, if currentView is "about", reset to "panel" on next open (same as Settings)

## Key Implementation Details from UI-SPEC.md
- Extend `currentView` type from `"panel" | "settings"` to `"panel" | "settings" | "about"`
- Extend slide container from `w-[200%]` to `w-[300%]`, each child `w-1/3`
- Third translate stop for "about": `-translate-x-[calc(66.667%+1.333rem)]`
- Header when `currentView === "about"`: show back arrow + title text from i18n
- Back button in About header returns to Settings: `setCurrentView("settings")`
- Add `openAbout: () => void` to `AppStateValue`

## Existing Code Patterns (from AppShell.tsx)
- Current view state: `const [currentView, setCurrentView] = useState<"panel" | "settings">("panel");`
- Slide container: `w-[200%]` with `gap-4` (16px)
- Panel view: `translate-x-0`
- Settings view: `-translate-x-[calc(50%+1rem)]`
- Back button in settings header: `setCurrentView("panel")`
- resetShellViewState: sets `currentView` to "panel"
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend currentView type in appState.ts</name>
  <files>src/app/shared/appState.ts</files>
  <action>
    Read src/app/shared/appState.ts to understand current structure.
    
    Update the AppStateValue interface:
    1. Change `currentView: "panel" | "settings"` to `currentView: "panel" | "settings" | "about"`
    2. Add `openAbout: () => void` to the interface
    
    The file currently has:
    - Line 14: `currentView: "panel" | "settings";`
    - Lines 23-24: `openSettings: () => void; closeSettings: () => void;`
    
    Add the new method after closeSettings.
  </action>
  <verify>
    <automated>grep -n 'currentView: "panel" | "settings" | "about"' src/app/shared/appState.ts && grep -n 'openAbout: () => void' src/app/shared/appState.ts</automated>
  </verify>
  <done>appState.ts has currentView type including "about" and openAbout method declared</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend AppShell for 3-view slide animation</name>
  <files>src/app/shell/AppShell.tsx</files>
  <action>
    Read src/app/shell/AppShell.tsx to understand current structure.
    
    Make the following changes:
    
    1. Update currentView state type (line ~79):
       FROM: `const [currentView, setCurrentView] = useState<"panel" | "settings">("panel");`
       TO: `const [currentView, setCurrentView] = useState<"panel" | "settings" | "about">("panel");`
    
    2. Update the slide container from 2-view to 3-view (around line ~612):
       - Change `w-[200%]` to `w-[300%]`
       - Change each child from `w-1/2` to `w-1/3`
       - Update translate classes:
         * Panel: `translate-x-0` (unchanged)
         * Settings: `-translate-x-[calc(33.333%+0.667rem)]` (was `-translate-x-[calc(50%+1rem)]`)
         * About: `-translate-x-[calc(66.667%+1.333rem)]` (new)
       
       The conditional class should handle three states:
       ```
       ${currentView === "panel" ? "translate-x-0" : currentView === "settings" ? "-translate-x-[calc(33.333%+0.667rem)]" : "-translate-x-[calc(66.667%+1.333rem)]"}
       ```
    
    3. Add About view container as third child (after Settings div):
       ```jsx
       <div
         ref={aboutScrollContainerRef}
         className="w-1/3 shrink-0 overflow-y-auto overflow-x-hidden pr-2"
         onScroll={(event) => setIsScrolled(event.currentTarget.scrollTop > 4)}
       >
         {/* AboutView will be rendered here in next plan */}
         <div className="flex h-full items-center justify-center text-slate-400">About page placeholder</div>
       </div>
       ```
       Also add `const aboutScrollContainerRef = useRef<HTMLDivElement>(null);` near other refs.
    
    4. Update header logic to handle "about" view (around line ~530):
       - When `currentView === "about"`, show back arrow + title
       - Back button should set `currentView("settings")` (not "panel")
       - Use copy.aboutTitle for the header text
       
    5. Update resetShellViewState to also reset about scroll container:
       Add: `if (aboutScrollContainerRef.current) { aboutScrollContainerRef.current.scrollTop = 0; }`
    
    6. Update AppStateContext.Provider value to include openAbout:
       Add: `openAbout: () => { setCurrentView("about"); setIsScrolled(false); }`
    
    7. Add effect to close promotion overlay when entering about view (similar to settings):
       ```jsx
       useEffect(() => {
         if (currentView !== "panel") {
           setPromotionOverlayState("closed");
         }
       }, [currentView]);
       ```
       (This may already exist and cover "about" since it checks !== "panel")
  </action>
  <verify>
    <automated>grep -n 'w-\[300%\]' src/app/shell/AppShell.tsx && grep -n 'w-1/3' src/app/shell/AppShell.tsx && grep -n '"about"' src/app/shell/AppShell.tsx | head -5</automated>
  </verify>
  <done>AppShell.tsx has 3-view slide container (300% width), about view slot, and openAbout in context</done>
</task>

</tasks>

<verification>
- TypeScript compilation passes: `npm run build` or `npx tsc --noEmit`
- AppShell renders without errors
- currentView type accepts "about" value
</verification>

<success_criteria>
- appState.ts has `currentView: "panel" | "settings" | "about"` type
- appState.ts has `openAbout: () => void` method in AppStateValue
- AppShell.tsx has `w-[300%]` slide container with three `w-1/3` children
- AppShell.tsx has translate-x classes for all three views
- AppShell.tsx header shows back button when currentView === "about"
- AppStateContext.Provider includes openAbout method
</success_criteria>

<output>
After completion, create `.planning/phases/06-about-page/06-01-SUMMARY.md`
</output>

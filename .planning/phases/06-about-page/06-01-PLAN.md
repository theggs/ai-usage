---
phase: 06-about-page
plan: 01
type: execute
wave: 1
depends_on: []
read_first:
  - .planning/phases/06-about-page/06-CONTEXT.md
  - .planning/phases/06-about-page/06-UI-SPEC.md
  - src/app/shared/appState.ts
  - src/app/shell/AppShell.tsx
files_modified:
  - src/app/shared/appState.ts
  - src/app/shell/AppShell.tsx
autonomous: true
requirements:
  - ABOUT-01
must_haves:
  truths:
    - currentView accepts "about" as a first-class shell view instead of treating About as Settings content
    - the shell can slide panel -> settings -> about and route back from about -> settings
    - existing keyboard shortcuts keep their current behavior and About introduces no new shortcut path
    - blur/reset logic returns the shell to panel and clears the about scroll position before the next open
  artifacts:
    - path: src/app/shared/appState.ts
      provides: AppStateValue contract with about view state plus openAbout and closeAbout helpers
    - path: src/app/shell/AppShell.tsx
      provides: Three-view shell container, about header branch, and placeholder about slot for later wiring
  key_links:
    - from: src/app/shared/appState.ts
      to: src/app/shell/AppShell.tsx
      via: AppStateContext.Provider currentView/openAbout/closeAbout contract
      pattern: 'openAbout|closeAbout'
    - from: src/app/shell/AppShell.tsx
      to: about view container
      via: translate classes and third scroll container
      pattern: 'w-\\[300%\\]|About page placeholder'
---

<objective>
Extend the shell from two views to three so About is an independent page per D-01/D-02, while keeping the existing slide mechanics and blur-to-panel reset behavior.

Purpose: ABOUT-01 is not satisfied until About has its own shell state and navigation path instead of living inside Settings.
Output: Updated app-state contract plus a three-view AppShell with an about placeholder slot ready for the real component in Plan 03.
</objective>

<execution_context>
@/Users/chasewang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chasewang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-about-page/06-CONTEXT.md
@.planning/phases/06-about-page/06-UI-SPEC.md

## Locked decisions to implement
- D-01: About is reached from the Settings footer only.
- D-02: Use the same slide animation pattern as panel -> settings.
- D-03: About back navigation returns to Settings, not Panel.
- D-04: No keyboard shortcut is added for About; existing F6/S and F7/B behavior stays unchanged.
- D-05: Blur/reset returns the shell to panel before the next reopen.

## Existing shell contract
- `src/app/shared/appState.ts` currently exposes `currentView`, `openSettings`, and `closeSettings`.
- `src/app/shell/AppShell.tsx` already owns the translate container, header switching, and blur reset behavior.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the app-state contract for About navigation</name>
  <files>src/app/shared/appState.ts</files>
  <action>Update `AppStateValue` so the shell contract explicitly supports About navigation. Change `currentView` from `"panel" | "settings"` to `"panel" | "settings" | "about"`, keep `openAbout`, and add `closeAbout: () => void` so the header/back flow is symmetrical with Settings. This implements D-03 and addresses the review note about the missing close helper.</action>
  <verify>
    <automated>rg -n 'currentView: "panel" \\| "settings" \\| "about"|openAbout: \\(\\) => void|closeAbout: \\(\\) => void' src/app/shared/appState.ts</automated>
  </verify>
  <done>`src/app/shared/appState.ts` exposes the about view state and both navigation helpers with no leftover two-view union.</done>
</task>

<task type="auto">
  <name>Task 2: Generalize AppShell to a three-view container with an About placeholder</name>
  <files>src/app/shell/AppShell.tsx</files>
  <action>Refactor the shell container so it can host panel, settings, and about. Change the translate strip from `w-[200%]` / `w-1/2` to `w-[300%]` / `w-1/3`, add the third translate stop for `currentView === "about"`, add `aboutScrollContainerRef`, and extend `resetShellViewState()` to reset that container along with the existing ones. Add an About header branch using `copy.aboutTitle` whose back button calls `closeAbout()` and returns to Settings per D-03. Expose `openAbout()` and `closeAbout()` from `AppStateContext.Provider`. Keep the existing keyboard handler behavior from `AppShell.tsx`: `F6` / `S` opens Settings only from panel, `F7` / `B` returns only from settings, and About must not introduce any new keyboard shortcut or hijack those existing keys per D-04. The third panel should render a simple "About page placeholder" body for now, and that placeholder should only mount when `currentView === "about"` so hidden About work does not start early. This keeps Plan 01 independent from Plan 03 while addressing the eager-render review concern.</action>
  <verify>
    <automated>node -e 'const fs=require("fs"); const text=fs.readFileSync("src/app/shell/AppShell.tsx","utf8"); if(!/w-\\[300%\\]|w-1\\/3|About page placeholder|aboutScrollContainerRef/.test(text)) process.exit(1); if(!/currentView === "panel".*F6|event\\.key === "F6"/s.test(text)) process.exit(1); if(!/currentView === "settings".*F7|event\\.key === "F7"/s.test(text)) process.exit(1); const keyHandler=text.match(/const handleKeyDown = \\(event: KeyboardEvent\\) => \\{([\\s\\S]*?)\\n    \\};/); if(!keyHandler) process.exit(1); if(/currentView === "about"|openAbout\\(/.test(keyHandler[1])) process.exit(1);'</automated>
  </verify>
  <done>`src/app/shell/AppShell.tsx` contains a working three-view shell, a dedicated About header/back path to Settings, a guarded placeholder slot, and unchanged keyboard shortcuts with no new About shortcut.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes after the app-state and shell updates.
- `rg -n 'About page placeholder' src/app/shell/AppShell.tsx` confirms the placeholder slot exists for Plan 03 to replace.
- The AppShell key handler still contains only the existing `F6` / `S` and `F7` / `B` flows, with no About-specific shortcut branch.
</verification>

<success_criteria>
- About is modeled as its own shell view, not a Settings subsection.
- The shell can translate to a third pane and back to Settings without losing the existing blur-to-panel behavior.
- About adds no new keyboard shortcut and does not change the existing Settings shortcut behavior.
- `openAbout` and `closeAbout` are both available through `useAppState()`.
</success_criteria>

<output>
After completion, create `.planning/phases/06-about-page/06-01-SUMMARY.md`
</output>

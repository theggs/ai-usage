---
phase: 05-time-aware-alert-thresholds
plan: 04
type: execute
wave: 1
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/phases/05-time-aware-alert-thresholds/05-RESEARCH.md
  - .planning/phases/05-time-aware-alert-thresholds/05-01-PLAN.md
  - .planning/phases/05-time-aware-alert-thresholds/05-01-SUMMARY.md
autonomous: true
requirements: [ALERT-03, ALERT-04]
gap_closure: true
must_haves:
  truths:
    - "Phase 05 documentation claims only the requirements the shipped risk-only UI actually satisfies: ALERT-03 and ALERT-04."
    - "ALERT-05 remains explicitly owned by Phase 4's visible burn-rate UI contract, not re-claimed by Phase 05."
    - "Phase 05 history still records that it reuses the three-bucket pace classifier while keeping healthy `on-track` rows visually quiet per D-03."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 05 requirement ledger aligned to ALERT-03 and ALERT-04, plus an explicit note that ALERT-05 stays in Phase 4"
      contains: "**Requirements**: ALERT-03, ALERT-04"
    - path: ".planning/phases/05-time-aware-alert-thresholds/05-RESEARCH.md"
      provides: "Research guidance that treats ALERT-05 as an upstream Phase 4 contract, not a Phase 5 requirement claim"
      contains: "Do not claim ALERT-05 in Phase 05 plan frontmatter"
    - path: ".planning/phases/05-time-aware-alert-thresholds/05-01-PLAN.md"
      provides: "Corrected Phase 05 plan frontmatter without ALERT-05"
      contains: "requirements: [ALERT-03, ALERT-04]"
    - path: ".planning/phases/05-time-aware-alert-thresholds/05-01-SUMMARY.md"
      provides: "Historical execution summary corrected to ALERT-03 and ALERT-04 with a Phase 4 ownership note for ALERT-05"
      contains: "requirements-completed: [ALERT-03, ALERT-04]"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "Phase 05 requirement line matches the traceability ledger that maps ALERT-05 to Phase 4"
      pattern: "ALERT-05 \\| Phase 4"
    - from: ".planning/phases/05-time-aware-alert-thresholds/05-CONTEXT.md"
      to: ".planning/phases/05-time-aware-alert-thresholds/05-01-PLAN.md"
      via: "D-03 keeps healthy on-track rows visually quiet, so the plan cannot claim a visible healthy badge requirement"
      pattern: "D-03"
    - from: ".planning/phases/04-burn-rate-engine/04-02-SUMMARY.md"
      to: ".planning/phases/05-time-aware-alert-thresholds/05-01-SUMMARY.md"
      via: "Phase 4 remains the source of the visible three-bucket pace contract that Phase 05 reuses"
      pattern: "On track"
---

<objective>
Resolve the Phase 05 verification blocker by aligning requirement bookkeeping with the approved risk-only UI.

Purpose: `05-VERIFICATION.md` found that `05-01-PLAN.md` incorrectly claimed `ALERT-05` even though `.planning/REQUIREMENTS.md` maps that requirement to Phase 4 and both Phase 4 and Phase 5 decisions intentionally keep healthy `on-track` rows visually quiet. Preserve the shipped Phase 05 behavior; fix the documentation and historical plan claims instead of reopening the UI.
Output: corrected roadmap/research/plan/summary artifacts that leave `ALERT-05` in Phase 4, keep Phase 05 on `ALERT-03` and `ALERT-04`, and explicitly document why the risk-only UI remains intentional.
</objective>

<execution_context>
@/Users/chasewang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chasewang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-time-aware-alert-thresholds/05-CONTEXT.md
@.planning/phases/05-time-aware-alert-thresholds/05-RESEARCH.md
@.planning/phases/05-time-aware-alert-thresholds/05-VERIFICATION.md
@.planning/phases/05-time-aware-alert-thresholds/05-01-PLAN.md
@.planning/phases/05-time-aware-alert-thresholds/05-01-SUMMARY.md
@.planning/phases/04-burn-rate-engine/04-CONTEXT.md
@.planning/phases/04-burn-rate-engine/04-02-SUMMARY.md

<interfaces>
From .planning/REQUIREMENTS.md:
```markdown
| ALERT-03 | Phase 5 | Complete |
| ALERT-04 | Phase 5 | Complete |
| ALERT-05 | Phase 4 | Complete |
```

From .planning/phases/05-time-aware-alert-thresholds/05-CONTEXT.md:
```markdown
- **D-03:** `On track` rows stay visually quiet. Phase 5 should not add a new healthy label where none is needed today.
```

From .planning/phases/04-burn-rate-engine/04-02-SUMMARY.md:
```markdown
- `On track` rows render no extra burn-rate block
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align Phase 05 source-of-truth planning docs to the approved Phase 4 ownership of ALERT-05</name>
  <files>.planning/ROADMAP.md, .planning/phases/05-time-aware-alert-thresholds/05-RESEARCH.md</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/05-time-aware-alert-thresholds/05-CONTEXT.md
    .planning/phases/05-time-aware-alert-thresholds/05-RESEARCH.md
    .planning/phases/05-time-aware-alert-thresholds/05-VERIFICATION.md
    .planning/phases/04-burn-rate-engine/04-CONTEXT.md
    .planning/phases/04-burn-rate-engine/04-02-SUMMARY.md
  </read_first>
  <action>
    Update the phase-level planning docs so they stop implying that Phase 05 owns `ALERT-05`.

    In `.planning/ROADMAP.md`:
    - Change the Phase 5 requirement line from `ALERT-03, ALERT-04, ALERT-05` to `ALERT-03, ALERT-04`.
    - Add one short traceability note under the Phase 5 success criteria stating that `ALERT-05` remains a Phase 4 burn-rate UI contract and Phase 5 only reuses that classifier while keeping healthy rows visually quiet per D-03.
    - Do not change the shipped Phase 5 success criteria or the overall phase goal.

    In `.planning/phases/05-time-aware-alert-thresholds/05-RESEARCH.md`:
    - Keep the `ALERT-03` and `ALERT-04` rows in `<phase_requirements>` unchanged.
    - Remove `ALERT-05` from the Phase 5 requirement table and replace it with a prose note immediately below the table that says `ALERT-05` stays mapped to Phase 4 in `.planning/REQUIREMENTS.md`.
    - In the "Planning Notes" section, replace the sentence that says `ALERT-05` should still be referenced with a sentence that explicitly says not to claim `ALERT-05` in Phase 5 plan frontmatter because the three-bucket pace model is an upstream Phase 4 contract and D-03 keeps healthy rows visually quiet.
    - Do not weaken or remove the existing D-03 / risk-only UI guidance.
  </action>
  <verify>
    <automated>rg -n "\\*\\*Requirements\\*\\*: ALERT-03, ALERT-04$|ALERT-05 remains a Phase 4 burn-rate UI contract|Do not claim ALERT-05 in Phase 05 plan frontmatter" .planning/ROADMAP.md .planning/phases/05-time-aware-alert-thresholds/05-RESEARCH.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "\\*\\*Requirements\\*\\*: ALERT-03, ALERT-04$" .planning/ROADMAP.md` returns the Phase 5 requirement line
    - `rg -n "ALERT-05 remains a Phase 4 burn-rate UI contract" .planning/ROADMAP.md` returns the new traceability note
    - `rg -n "ALERT-05 stays mapped to Phase 4" .planning/phases/05-time-aware-alert-thresholds/05-RESEARCH.md` returns the research note below the requirement table
    - `rg -n "Do not claim ALERT-05 in Phase 05 plan frontmatter" .planning/phases/05-time-aware-alert-thresholds/05-RESEARCH.md` returns the updated planning note
    - `rg -n "ALERT-05 \\| Phase 4 \\| Complete" .planning/REQUIREMENTS.md` still returns the existing traceability row unchanged
  </acceptance_criteria>
  <done>
    The roadmap and research artifacts now agree that Phase 05 owns only `ALERT-03` and `ALERT-04`, while `ALERT-05` remains explicitly tied to Phase 4's visible burn-rate UI contract.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct the executed Phase 05 plan history so it no longer claims ALERT-05</name>
  <files>.planning/phases/05-time-aware-alert-thresholds/05-01-PLAN.md, .planning/phases/05-time-aware-alert-thresholds/05-01-SUMMARY.md</files>
  <read_first>
    .planning/phases/05-time-aware-alert-thresholds/05-01-PLAN.md
    .planning/phases/05-time-aware-alert-thresholds/05-01-SUMMARY.md
    .planning/phases/05-time-aware-alert-thresholds/05-VERIFICATION.md
    .planning/phases/05-time-aware-alert-thresholds/05-CONTEXT.md
    .planning/phases/04-burn-rate-engine/04-02-SUMMARY.md
  </read_first>
  <action>
    Patch the historical Phase 05 artifacts so they preserve what shipped without continuing the invalid requirement claim.

    In `.planning/phases/05-time-aware-alert-thresholds/05-01-PLAN.md`:
    - Change frontmatter `requirements` from `[ALERT-03, ALERT-04, ALERT-05]` to `[ALERT-03, ALERT-04]`.
    - In `<objective>`, replace the phrase that frames the work as keeping the `ALERT-05` contract with wording that says Plan 01 reuses the existing three-bucket pace classifier from Phase 4 as an input contract while Phase 5 focuses on `ALERT-03` and `ALERT-04`.
    - Leave the technical classifier behavior intact; do not rewrite the plan into a different implementation history.

    In `.planning/phases/05-time-aware-alert-thresholds/05-01-SUMMARY.md`:
    - Change frontmatter `requirements-completed` from `[ALERT-03, ALERT-04, ALERT-05]` to `[ALERT-03, ALERT-04]`.
    - Add one explicit bookkeeping note in the body saying Phase 05 consumes the three-bucket pace classifier but `ALERT-05` remains satisfied by Phase 4 because the shipped Phase 05 UI intentionally keeps `on-track` rows visually quiet.
    - Do not remove the existing classifier accomplishments or test-history notes.
  </action>
  <verify>
    <automated>rg -n "requirements: \\[ALERT-03, ALERT-04\\]|requirements-completed: \\[ALERT-03, ALERT-04\\]|Phase 05 consumes the three-bucket pace classifier but ALERT-05 remains satisfied by Phase 4" .planning/phases/05-time-aware-alert-thresholds/05-01-PLAN.md .planning/phases/05-time-aware-alert-thresholds/05-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "requirements: \\[ALERT-03, ALERT-04\\]" .planning/phases/05-time-aware-alert-thresholds/05-01-PLAN.md` returns the corrected frontmatter line
    - `rg -n "focuses on `ALERT-03` and `ALERT-04`" .planning/phases/05-time-aware-alert-thresholds/05-01-PLAN.md` returns the revised objective wording
    - `rg -n "requirements-completed: \\[ALERT-03, ALERT-04\\]" .planning/phases/05-time-aware-alert-thresholds/05-01-SUMMARY.md` returns the corrected summary frontmatter line
    - `rg -n "Phase 05 consumes the three-bucket pace classifier but ALERT-05 remains satisfied by Phase 4" .planning/phases/05-time-aware-alert-thresholds/05-01-SUMMARY.md` returns the new bookkeeping note
    - `rg -n "On track rows stay visually quiet|on-track rows visually quiet" .planning/phases/05-time-aware-alert-thresholds/05-CONTEXT.md .planning/phases/04-burn-rate-engine/04-02-SUMMARY.md` still returns the unchanged design-intent evidence
  </acceptance_criteria>
  <done>
    The executed Phase 05 history now records the real scope: Plan 01 delivered the shared classifier and fallback logic for `ALERT-03` and `ALERT-04`, while the visible healthy-state requirement stays with Phase 4.
  </done>
</task>

</tasks>

<verification>
  <criteria>
    - `.planning/ROADMAP.md` no longer lists `ALERT-05` under Phase 5 requirements
    - `.planning/phases/05-time-aware-alert-thresholds/05-RESEARCH.md` explicitly says not to claim `ALERT-05` in Phase 5 plan frontmatter
    - `.planning/phases/05-time-aware-alert-thresholds/05-01-PLAN.md` and `05-01-SUMMARY.md` both list only `ALERT-03` and `ALERT-04`
    - `.planning/REQUIREMENTS.md` continues to map `ALERT-05` to Phase 4 without modification
  </criteria>
</verification>

<success_criteria>
The Phase 05 verification blocker is removed without changing shipped UI behavior: Phase 05 documentation consistently claims `ALERT-03` and `ALERT-04`, `ALERT-05` remains traceable to Phase 4, and the approved risk-only `on-track` silence stays explicitly intentional.
</success_criteria>

<output>
After completion, create `.planning/phases/05-time-aware-alert-thresholds/05-04-SUMMARY.md`
</output>

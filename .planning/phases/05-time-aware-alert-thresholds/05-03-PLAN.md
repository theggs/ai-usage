---
phase: 05-time-aware-alert-thresholds
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src-tauri/src/tray/mod.rs
  - src-tauri/src/commands/mod.rs
autonomous: true
requirements: [ALERT-03, ALERT-04]
must_haves:
  truths:
    - "Tray severity follows the same pace-first, fallback-second policy as the frontend instead of lowest remaining percent only."
    - "A high remaining percentage can still produce tray danger when reset is imminent and the existing Phase 4 pace math implies far-behind."
    - "Missing or invalid `resets_at` falls back to the existing static percentage thresholds with no empty tray severity."
    - "Provider fetchers stay untouched; tray alignment lives in shared Rust normalization or tray-local helpers instead of per-provider duplication."
  artifacts:
    - path: "src-tauri/src/tray/mod.rs"
      provides: "Rust tray severity helper that mirrors the Phase 05 pace-aware rule"
      contains: "tray_dimension_severity"
    - path: "src-tauri/src/tray/mod.rs"
      provides: "Regression coverage for pace-danger, pace-normal, and fallback behavior"
      contains: "derives_tray_severity"
    - path: "src-tauri/src/commands/mod.rs"
      provides: "Cached tray-item path remains compatible with the new tray severity inputs"
      contains: "build_tray_items"
  key_links:
    - from: "src-tauri/src/tray/mod.rs"
      to: "src/lib/tauri/burnRate.ts"
      via: "Rust mirrors the same Phase 4 whole-window rule without changing provider fetchers"
      pattern: "remaining_percent"
    - from: "src-tauri/src/tray/mod.rs"
      to: "src-tauri/src/commands/mod.rs"
      via: "Live and cached tray item flows both feed the same severity helper"
      pattern: "build_tray_items"
---

<objective>
Align the Rust tray behavior with the Phase 05 health model.

Purpose: satisfy the tray side of `ALERT-03` and `ALERT-04` so the menu bar icon and severity no longer disagree with the React panel when pace changes urgency.
Output: a Rust tray severity helper that mirrors the pace-first fallback-second rule, plus targeted Rust tests for live and cached tray inputs.
</objective>

<execution_context>
@/Users/chasewang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chasewang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-time-aware-alert-thresholds/05-CONTEXT.md
@.planning/phases/05-time-aware-alert-thresholds/05-RESEARCH.md
@.planning/phases/05-time-aware-alert-thresholds/05-VALIDATION.md
@.planning/phases/05-time-aware-alert-thresholds/05-01-PLAN.md

<interfaces>
From [src-tauri/src/tray/mod.rs](/Users/chasewang/01workspace/projects/ai-usage/src-tauri/src/tray/mod.rs):
```rust
fn tray_severity(items: &[PanelPlaceholderItem]) -> &'static str {
    let dimensions = all_dimensions(items);
    if dimensions
        .iter()
        .any(|dimension| matches!(dimension.remaining_percent, Some(percent) if percent < 20))
    {
        return "danger";
    }
    ...
}
```

From [src-tauri/src/commands/mod.rs](/Users/chasewang/01workspace/projects/ai-usage/src-tauri/src/commands/mod.rs):
```rust
pub fn build_tray_items(
    preferences: &UserPreferences,
    live_states: &[(&str, &CodexPanelState)],
    refreshed_at: &str,
) -> Vec<PanelPlaceholderItem> { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace lowest-percent tray severity with a pace-aware per-dimension helper</name>
  <files>src-tauri/src/tray/mod.rs</files>
  <read_first>
    src-tauri/src/tray/mod.rs
    src-tauri/src/commands/mod.rs
    .planning/phases/05-time-aware-alert-thresholds/05-CONTEXT.md
    .planning/phases/05-time-aware-alert-thresholds/05-RESEARCH.md
  </read_first>
  <action>
Implement the Rust tray rule in `src-tauri/src/tray/mod.rs` without touching provider-specific modules.

Add a small helper stack near `tray_severity(...)` with these exact semantics:

```rust
fn tray_dimension_severity(
    dimension: &crate::state::QuotaDimension,
    now_ms: i64,
) -> &'static str { ... }
```

The helper must mirror the frontend policy from Plan 01:
- if `remaining_percent` is `None`, return `"normal"`
- if `resets_at` is missing, invalid, in the past, or farther in the future than the supported window length, fall back to static thresholds:
  - `> 50` => `"normal"`
  - `20..=50` => `"warning"`
  - `< 20` => `"danger"`
- if `label` identifies a supported window and `resets_at` is valid:
  - compute whole-window elapsed time and coverage using the same Phase 4 rule already documented in research
  - `coverage >= 1.0` => `"normal"`
  - `coverage >= 0.5 && coverage < 1.0` => `"warning"`
  - `coverage < 0.5` => `"danger"`

Use the same supported-window policy as the frontend:
- `5h`
- `week`
- `7d`

Then rewrite `tray_severity(items)` so it:
- flattens dimensions
- returns `"empty"` when there are none
- otherwise computes each dimension's severity with `tray_dimension_severity(...)`
- returns the worst of those values in priority order `danger > warning > normal`

Keep tray summary text, icon tinting, and auto-selection behavior unchanged.
  </action>
  <verify>
    <automated>cargo test tray --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "fn tray_dimension_severity" src-tauri/src/tray/mod.rs` returns one match
    - `rg -n "coverage|resets_at|week|7d|5h" src-tauri/src/tray/mod.rs` returns matches inside the new helper logic
    - `rg -n "tray_dimension_severity\\(" src-tauri/src/tray/mod.rs` returns matches from `tray_severity(...)`
    - `cargo test tray --manifest-path src-tauri/Cargo.toml` passes
  </acceptance_criteria>
  <done>
    Tray severity now reflects actual time-aware quota health instead of relying only on the lowest remaining percentage.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Rust regression tests for pace danger, pace normal, and fallback tray behavior</name>
  <files>src-tauri/src/tray/mod.rs, src-tauri/src/commands/mod.rs</files>
  <read_first>
    src-tauri/src/tray/mod.rs
    src-tauri/src/commands/mod.rs
    .planning/phases/05-time-aware-alert-thresholds/05-VALIDATION.md
  </read_first>
  <action>
Extend the existing Rust tests with explicit Phase 05 cases.

In `src-tauri/src/tray/mod.rs` tests, add these exact scenarios:
1. `remaining_percent: Some(80)`, `label: "codex / 5h"`, `resets_at` 5 minutes in the future => `tray_severity(...) == "danger"`
2. `remaining_percent: Some(20)`, `label: "codex / week"`, `resets_at` 6 hours in the future => `tray_severity(...) == "normal"`
3. `remaining_percent: Some(45)`, `label: "codex / 5h"`, `resets_at: None` => `tray_severity(...) == "warning"`
4. `remaining_percent: Some(80)`, `label: "codex / 5h"`, `resets_at` beyond 5 hours => `tray_severity(...) == "normal"`
5. empty items => `tray_severity(...) == "empty"` remains unchanged

Update or replace the old `derives_tray_severity_from_lowest_percent` test so its name and assertions reflect the new pace-aware rule rather than the obsolete percent-only assumption.

In `src-tauri/src/commands/mod.rs`, add one focused regression named `build_tray_items_preserves_reset_metadata_for_time_aware_tray_severity` that `build_tray_items(...)` still returns quota dimensions carrying `label`, `remaining_percent`, and `resets_at` values needed by the new tray helper for cached snapshot states. Do not change the command behavior; just lock the input contract the tray now depends on.
  </action>
  <verify>
    <automated>cargo test tray --manifest-path src-tauri/Cargo.toml && cargo test build_tray_items --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "derives_tray_severity_from_time_aware|fallback" src-tauri/src/tray/mod.rs` returns Phase 05 test coverage
    - `rg -n "build_tray_items_preserves_reset_metadata_for_time_aware_tray_severity" src-tauri/src/commands/mod.rs` returns the new contract regression
    - `cargo test tray --manifest-path src-tauri/Cargo.toml && cargo test build_tray_items --manifest-path src-tauri/Cargo.toml` passes
  </acceptance_criteria>
  <done>
    Rust tests now protect the tray from regressing back to lowest-percent severity, and cached tray inputs remain compatible with the new logic.
  </done>
</task>

</tasks>

<verification>
  <criteria>
    - `tray_severity(...)` uses pace-aware logic when supported data exists
    - Missing or invalid reset timestamps still fall back to the static percentage thresholds
    - Existing cached-tray paths remain compatible with the new helper
  </criteria>
</verification>

---
phase: 05-time-aware-alert-thresholds
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/shared/i18n.ts
  - src/app/shared/i18n.test.ts
  - src/components/panel/QuotaSummary.tsx
  - src/components/panel/ServiceCard.tsx
  - src/components/panel/ServiceCard.test.tsx
  - src/app/panel/PanelView.test.tsx
  - src/app/shell/AppShell.tsx
  - src/app/shell/AppShell.test.tsx
autonomous: false
requirements: [ALERT-03, ALERT-04]
must_haves:
  truths:
    - "Rows with valid pace data show pace wording directly on the severity badge and no longer render the old inline burn-rate block."
    - "Rows without valid pace data still show the old static warning labels and tones when percentage thresholds require it."
    - "Service-card header badge, card accent, and top summary all follow the same worst-row health decision from Plan 01."
    - "ETA detail is discoverable from the pace disclosure without taking a permanent extra line in the row body."
    - "Frontend SnapshotStatus switches touched during this plan are exhaustive instead of hiding future variants behind silent defaults."
  artifacts:
    - path: "src/components/panel/QuotaSummary.tsx"
      provides: "Row badge and compact hover-only ETA behavior"
      contains: "getQuotaHealthSignal"
    - path: "src/components/panel/ServiceCard.tsx"
      provides: "Worst-row header badge and accent based on pace-aware health"
      contains: "getMostUrgentQuotaDimension"
    - path: "src/app/shell/AppShell.tsx"
      provides: "Panel summary wording that distinguishes pace-driven and fallback-driven warning states"
      contains: "panelPaceDangerSummary"
    - path: "src/app/shared/i18n.ts"
      provides: "Localized pace-summary strings and exhaustive helper branches"
      contains: "panelPaceWarningSummary"
  key_links:
    - from: "src/components/panel/QuotaSummary.tsx"
      to: "src/lib/tauri/summary.ts"
      via: "Row rendering consumes the shared Phase 05 quota-health signal"
      pattern: "getQuotaHealthSignal"
    - from: "src/components/panel/ServiceCard.tsx"
      to: "src/lib/tauri/summary.ts"
      via: "Header badge and card accent consume the most urgent row helper"
      pattern: "getMostUrgentQuotaDimension"
    - from: "src/app/shell/AppShell.tsx"
      to: "src/app/shared/i18n.ts"
      via: "Summary line switches between pace-aware and fallback wording"
      pattern: "panelPaceDangerSummary"
---

<objective>
Migrate the React surfaces to the Phase 05 health model.

Purpose: satisfy the user-visible part of `ALERT-03` and `ALERT-04` by replacing static row/card/panel warning wording with pace-aware behavior when available, while preserving static fallback labels when time-aware input is unavailable.
Output: compact row badges, hover-only ETA disclosure, pace-aware card/panel aggregation, updated localized copy, exhaustive frontend status helpers, and automated plus manual verification.
</objective>

<execution_context>
@/Users/chasewang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chasewang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-time-aware-alert-thresholds/05-CONTEXT.md
@.planning/phases/05-time-aware-alert-thresholds/05-RESEARCH.md
@.planning/phases/05-time-aware-alert-thresholds/05-VALIDATION.md
@.planning/phases/05-time-aware-alert-thresholds/05-01-PLAN.md

<interfaces>
From [src/components/panel/QuotaSummary.tsx](/Users/chasewang/01workspace/projects/ai-usage/src/components/panel/QuotaSummary.tsx):
```typescript
const severityLabel =
  dimension.status === "exhausted"
    ? localizeStatusLabel(copy, "danger")
    : dimension.status === "warning"
      ? localizeStatusLabel(copy, "warning")
      : undefined;
```

From [src/components/panel/ServiceCard.tsx](/Users/chasewang/01workspace/projects/ai-usage/src/components/panel/ServiceCard.tsx):
```typescript
const alertLevel = getServiceAlertLevel(service);
const severityLabel = localizeStatusLabel(copy, getSeverityLabelKey(service));
```

From [src/app/shell/AppShell.tsx](/Users/chasewang/01workspace/projects/ai-usage/src/app/shell/AppShell.tsx):
```typescript
const panelSummary = getPanelHealthSummary(visibleItems);
const summaryText = panelSummary.tone === "danger"
  ? copy.panelDangerSummary ...
  : copy.panelWarningSummary ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace row and card severity surfaces with pace-aware badges and compact hover-only ETA disclosure</name>
  <files>src/app/shared/i18n.ts, src/app/shared/i18n.test.ts, src/components/panel/QuotaSummary.tsx, src/components/panel/ServiceCard.tsx, src/components/panel/ServiceCard.test.tsx, src/app/panel/PanelView.test.tsx</files>
  <read_first>
    src/app/shared/i18n.ts
    src/app/shared/i18n.test.ts
    src/components/panel/QuotaSummary.tsx
    src/components/panel/ServiceCard.tsx
    src/components/panel/ServiceCard.test.tsx
    src/app/panel/PanelView.test.tsx
    src/lib/tauri/summary.ts
    .planning/phases/05-time-aware-alert-thresholds/05-CONTEXT.md
  </read_first>
  <action>
Implement the row and card migration exactly as follows.

In `src/app/shared/i18n.ts`, add these new copy keys to `CopyTree`, the base locale, and `zh-CN`:
```typescript
panelPaceWarningSummary: "{service} {dimension} burning too fast" / "{service}{dimension}消耗偏快"
panelPaceDangerSummary: "{service} {dimension} burning far too fast" / "{service}{dimension}消耗过快"
```

Keep the existing `panelWarningSummary` and `panelDangerSummary` unchanged for fallback-only rows.

In `src/components/panel/QuotaSummary.tsx`:
- import `getQuotaHealthSignal` from `src/lib/tauri/summary.ts`
- keep `const burnRate = getQuotaBurnRateDisplay(dimension, nowMs)` so the badge can still expose ETA detail on hover
- compute `const health = getQuotaHealthSignal(dimension, nowMs)`
- replace row color usage so `localizedRemaining`, progress fill color, and text tone use `health.progressTone` instead of raw `dimension.progressTone`
- derive badge text with these exact rules:
  - `health.source === "pace"` and `health.pace === "behind"` or `"far-behind"` => badge text is `localizeBurnRatePace(copy, health.pace)`
  - `health.source === "fallback"` and `health.level !== "normal"` => badge text is `localizeStatusLabel(copy, health.level === "danger" ? "danger" : "warning")`
  - all other cases => no badge
- delete the existing `showBurnRateBlock` branch and remove the inline pace line plus secondary line from the row body entirely
- keep the reset hint line only; do not restore a second permanent line for ETA
- when the badge comes from `source === "pace"` and `burnRate?.willLastUntilReset === false`, expose the ETA detail via the badge `title` and `aria-label` using `localizeBurnRateSecondaryLine(copy, burnRate)`
- keep healthy pace rows visually quiet: no badge for `pace === "on-track"`

In `src/components/panel/ServiceCard.tsx`:
- import and use `getMostUrgentQuotaDimension([service], nowMs)` from Plan 01
- use the selected candidate's `health.level` for `cardClass` and `accentClass`
- use the selected candidate's `health` for the header badge text with the same mapping rules as row badges
- keep `badgeLabel` / snapshot freshness badge behavior unchanged

Update tests:
- `src/app/shared/i18n.test.ts` must assert the exact new panel pace summary strings in both locales
- `src/components/panel/ServiceCard.test.tsx` must cover:
  - a pace-danger row makes the card accent and header badge danger
  - a fallback-warning row still shows the old static warning label
  - an on-track row shows no pace badge
- `src/app/panel/PanelView.test.tsx` must assert the old inline burn-rate secondary line is absent from quota rows after the migration
  - use negative assertions against `Runs out in ~` / `约 .* 后用尽` in row body text
  - keep a positive assertion that reset hints still render
  </action>
  <verify>
    <automated>npx vitest run src/app/shared/i18n.test.ts src/components/panel/ServiceCard.test.tsx src/app/panel/PanelView.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "panelPaceWarningSummary|panelPaceDangerSummary" src/app/shared/i18n.ts` returns matches in `CopyTree`, base copy, and zh-CN copy
    - `rg -n "getQuotaHealthSignal" src/components/panel/QuotaSummary.tsx` returns one match
    - `rg -n "title=|aria-label=|localizeBurnRateSecondaryLine" src/components/panel/QuotaSummary.tsx` returns the pace disclosure badge wiring
    - `rg -n "showBurnRateBlock" src/components/panel/QuotaSummary.tsx` does not return a match
    - `rg -n "burning too fast|burning far too fast|消耗偏快|消耗过快" src/app/shared/i18n.test.ts src/components/panel/ServiceCard.test.tsx` returns matches
    - `npx vitest run src/app/shared/i18n.test.ts src/components/panel/ServiceCard.test.tsx src/app/panel/PanelView.test.tsx` passes
  </acceptance_criteria>
  <done>
    Quota rows are compact again, pace detail lives on hover instead of an extra line, and service-card severity follows the same worst-row health signal as the row badges.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update top-summary wording and make touched frontend SnapshotStatus switches exhaustive</name>
  <files>src/app/shared/i18n.ts, src/app/shell/AppShell.tsx, src/app/shell/AppShell.test.tsx, src/lib/tauri/summary.ts</files>
  <read_first>
    src/app/shared/i18n.ts
    src/app/shell/AppShell.tsx
    src/app/shell/AppShell.test.tsx
    src/lib/tauri/summary.ts
    .planning/phases/05-time-aware-alert-thresholds/05-CONTEXT.md
    .planning/phases/05-time-aware-alert-thresholds/05-RESEARCH.md
  </read_first>
  <action>
Update the panel summary and exhaustiveness exactly as follows.

In `src/app/shell/AppShell.tsx`:
- keep `const panelSummary = getPanelHealthSummary(visibleItems, displayNowMs)`
- replace the current string assembly with source-aware wording:
  - `panelSummary.tone === "empty"` => `copy.noServicesConnected`
  - `panelSummary.tone === "healthy"` => `copy.allServicesHealthy`
  - `panelSummary.source === "pace" && panelSummary.pace === "behind"` => `copy.panelPaceWarningSummary`
  - `panelSummary.source === "pace" && panelSummary.pace === "far-behind"` => `copy.panelPaceDangerSummary`
  - otherwise keep the existing fallback `panelWarningSummary` / `panelDangerSummary`
- preserve the current `{service}` and localized `{dimension}` replacement behavior

In `src/app/shared/i18n.ts` and `src/lib/tauri/summary.ts`, remove silent default fallbacks from the `SnapshotStatus` switch statements touched by this plan:
- `getPlaceholderCopy(...)`
- `statusToConnectionState(...)`
- `statusToPrimaryMessage(...)`

Use an explicit exhaustiveness helper pattern instead of `default`, for example:
```typescript
const assertNever = (value: never): never => {
  throw new Error(`Unhandled value: ${JSON.stringify(value)}`);
};
```
Apply it to the discriminated union object, not to `status.kind` as a raw string.

Add tests in `src/app/shell/AppShell.test.tsx` for:
1. pace-danger summary uses `panelPaceDangerSummary`
2. fallback-warning summary still uses the existing `panelWarningSummary`
3. healthy summary still uses `allServicesHealthy`

Do not change connection-state semantics or placeholder copy text during the exhaustiveness refactor; only remove the silent fallback risk and update the top-summary wording.
  </action>
  <verify>
    <automated>npx vitest run src/app/shell/AppShell.test.tsx src/app/shared/i18n.test.ts src/lib/tauri/summary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "panelPaceWarningSummary|panelPaceDangerSummary" src/app/shell/AppShell.tsx` returns matches
    - `rg -n "assertNever" src/app/shared/i18n.ts src/lib/tauri/summary.ts` returns matches
    - `rg -n "default:" src/app/shared/i18n.ts src/lib/tauri/summary.ts` does not return the touched `SnapshotStatus` switch branches
    - `rg -n "panelPaceDangerSummary|panelWarningSummary|allServicesHealthy" src/app/shell/AppShell.test.tsx` returns matches
    - `npx vitest run src/app/shell/AppShell.test.tsx src/app/shared/i18n.test.ts src/lib/tauri/summary.test.ts` passes
  </acceptance_criteria>
  <done>
    The panel header now distinguishes pace-driven urgency from static fallback warnings, and the touched frontend status helpers fail loudly instead of silently swallowing new `SnapshotStatus` variants.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify compact badge disclosure and aggregate consistency in the live panel</name>
  <files>src/components/panel/QuotaSummary.tsx, src/components/panel/ServiceCard.tsx, src/app/shell/AppShell.tsx</files>
  <read_first>
    .planning/phases/05-time-aware-alert-thresholds/05-CONTEXT.md
    .planning/phases/05-time-aware-alert-thresholds/05-VALIDATION.md
    src/components/panel/QuotaSummary.tsx
    src/components/panel/ServiceCard.tsx
    src/app/shell/AppShell.tsx
  </read_first>
  <action>
After Tasks 1 and 2 pass their automated checks, run the app and manually verify the visual contract that automation cannot fully prove:
1. Open the panel in `zh-CN` and `en-US`.
2. Confirm a pace-warning or pace-danger row shows a compact pace badge and no inline ETA line below the progress bar.
3. Hover the pace badge and confirm ETA detail appears there instead of in the row body.
4. Confirm a fallback-warning row still shows the old static warning label.
5. Confirm the service-card header badge, left accent, and top summary all point at the same worst visible row.

If any of those fail, fix them in this plan before approval. Do not introduce a new tooltip library or animation for this checkpoint.
  </action>
  <verify>
    <automated>npx vitest run src/app/shared/i18n.test.ts src/components/panel/ServiceCard.test.tsx src/app/panel/PanelView.test.tsx src/app/shell/AppShell.test.tsx</automated>
  </verify>
  <how-to-verify>
    1. Use one provider row that produces `behind` or `far-behind` and one row that falls back to the old static threshold path.
    2. In `zh-CN`, confirm `消耗偏快` / `消耗过快` appears on the badge and the row body stays compact.
    3. In `en-US`, confirm the badge hover exposes ETA detail and the panel summary uses the new pace wording.
    4. Confirm there is no restored persistent `Runs out in ~...` line inside the row body.
  </how-to-verify>
  <acceptance_criteria>
    - `npx vitest run src/app/shared/i18n.test.ts src/components/panel/ServiceCard.test.tsx src/app/panel/PanelView.test.tsx src/app/shell/AppShell.test.tsx` passes before manual review
    - human review confirms hover ETA is discoverable and the row body is compact in both supported locales
  </acceptance_criteria>
  <done>
    The live panel matches the Phase 05 compact disclosure contract and all aggregate React surfaces stay in sync with the same worst-row decision.
  </done>
</task>

</tasks>

<verification>
  <criteria>
    - Row badges use pace wording when valid and static wording when not
    - The old inline burn-rate block is removed from quota rows
    - Service-card and top-summary aggregation use the shared Phase 05 classifier
    - Touched frontend `SnapshotStatus` switches are exhaustive
  </criteria>
</verification>

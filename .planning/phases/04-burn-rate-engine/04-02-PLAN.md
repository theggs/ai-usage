---
phase: 04-burn-rate-engine
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/shared/i18n.ts
  - src/app/shared/i18n.test.ts
  - src/components/panel/QuotaSummary.tsx
  - src/components/panel/ServiceCard.test.tsx
  - src/app/panel/PanelView.test.tsx
  - src/features/demo-services/demoData.ts
autonomous: false
requirements: [ALERT-01, ALERT-02]
must_haves:
  truths:
    - "Each visible quota row can show its own burn-rate pace label plus one short secondary line."
    - "Rows projected to last show a positive confirmation instead of an ETA countdown."
    - "Rows projected to deplete early show a compact human-readable ETA."
    - "Rows with missing history or invalid/missing resetsAt show no burn-rate block at all."
    - "The new burn-rate copy fits the existing compact quota-row layout in both zh-CN and en-US."
  artifacts:
    - path: "src/app/shared/i18n.ts"
      provides: "Localized burn-rate labels and compact relative-time formatter"
      contains: "burnRateOnTrack"
    - path: "src/components/panel/QuotaSummary.tsx"
      provides: "Two-line burn-rate rendering inside each quota block"
      contains: "burnRate"
    - path: "src/components/panel/ServiceCard.test.tsx"
      provides: "Quota-row render coverage for burn-rate visible and hidden states"
      contains: "Will last until reset"
    - path: "src/app/panel/PanelView.test.tsx"
      provides: "Panel-level regression coverage for burn-rate rendering with multiple rows"
      contains: "Runs out in ~"
  key_links:
    - from: "src/components/panel/QuotaSummary.tsx"
      to: "src/lib/tauri/burnRate.ts"
      via: "getBurnRateDisplay or summary wrapper call at render time"
      pattern: "getBurnRateDisplay"
    - from: "src/components/panel/QuotaSummary.tsx"
      to: "src/app/shared/i18n.ts"
      via: "localized pace label and secondary line formatting"
      pattern: "localizeBurnRate"
    - from: "src/features/demo-services/demoData.ts"
      to: "src/components/panel/QuotaSummary.tsx"
      via: "demo quota dimensions include burnRateHistory samples for panel regressions"
      pattern: "burnRateHistory"
---

<objective>
Wire the burn-rate engine into the panel UI with compact localized copy and an explicit human verification gate.

Purpose: Complete `ALERT-01` and `ALERT-02` by rendering per-dimension pace plus ETA/positive confirmation inside `QuotaSummary`, while preserving all existing card layout, progress colors, and degraded-state silence per D-08 through D-13.
Output: Localized burn-rate copy, quota-row rendering, updated tests, and one human verification checkpoint for menubar readability.
</objective>

<execution_context>
@/Users/chasewang/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chasewang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-burn-rate-engine/04-CONTEXT.md
@.planning/phases/04-burn-rate-engine/04-RESEARCH.md
@.planning/phases/04-burn-rate-engine/04-UI-SPEC.md
@.planning/phases/04-burn-rate-engine/04-VALIDATION.md
@.planning/phases/04-burn-rate-engine/04-01-PLAN.md

<interfaces>
From [src/components/panel/QuotaSummary.tsx](/Users/chasewang/01workspace/projects/ai-usage/src/components/panel/QuotaSummary.tsx):
```typescript
export const QuotaSummary = ({
  dimension,
  copy,
  nowMs
}: {
  dimension: QuotaDimension;
  copy: CopyTree;
  nowMs?: number;
}) => { ... }
```

From [src/app/shared/i18n.ts](/Users/chasewang/01workspace/projects/ai-usage/src/app/shared/i18n.ts):
```typescript
export const localizeResetHint = (copy: CopyTree, value?: string, nowMs = Date.now()): string | undefined => { ... }
export const localizeDimensionLabel = (copy: CopyTree, label: string): string => { ... }
```

From [src/features/demo-services/demoData.ts](/Users/chasewang/01workspace/projects/ai-usage/src/features/demo-services/demoData.ts):
```typescript
{
  label: "Local Messages / 5h",
  remainingPercent: 64,
  remainingAbsolute: "64% remaining",
  resetsAt: minutesFromNow(120)
}.map(decorateQuotaDimension)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compact localized burn-rate copy and formatting helpers</name>
  <files>src/app/shared/i18n.ts, src/app/shared/i18n.test.ts</files>
  <read_first>
    src/app/shared/i18n.ts
    src/app/shared/i18n.test.ts
    .planning/phases/04-burn-rate-engine/04-CONTEXT.md
    .planning/phases/04-burn-rate-engine/04-UI-SPEC.md
  </read_first>
  <action>
Extend `CopyTree` and both locale trees in `src/app/shared/i18n.ts` with the exact compact keys required by the UI spec:
```typescript
burnRateOnTrack: "On track" / "进度正常"
burnRateBehind: "Behind" / "消耗偏快"
burnRateFarBehind: "Far behind" / "消耗过快"
burnRateWillLastUntilReset: "Will last until reset" / "可撑到重置"
burnRateRunsOutInFormat: "Runs out in ~{value}" / "约 {value} 后用尽"
```

Add two helpers:
```typescript
export const localizeBurnRatePace = (
  copy: CopyTree,
  pace: "on-track" | "behind" | "far-behind"
): string => { ... };

export const localizeBurnRateSecondaryLine = (
  copy: CopyTree,
  burnRate: { willLastUntilReset: boolean; depletionEtaMs: number | null }
): string => { ... };
```

`localizeBurnRateSecondaryLine` must format compact values only:
- if `willLastUntilReset` is `true`, return `copy.burnRateWillLastUntilReset`
- otherwise convert `depletionEtaMs` into the existing short units already used in this file:
  - `< 60 minutes` => `Xm`
  - `< 24 hours` => `Xh YYm`
  - `>= 24 hours` => `Xd YYh`
- then substitute the compact value into `burnRateRunsOutInFormat`

Keep copy compact per D-09 and UI spec:
- no trailing periods
- no placeholder strings like `Calculating...`
- no new color or severity wording
Add unit tests in `src/app/shared/i18n.test.ts` covering:
- en-US and zh-CN pace labels
- positive confirmation text
- ETA text for minute, hour, and day ranges
- no expanded prose beyond the exact compact strings above
  </action>
  <verify>
    <automated>npx vitest run src/app/shared/i18n.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "burnRateOnTrack" src/app/shared/i18n.ts` returns matches in `CopyTree`, base copy, and zh-CN copy
    - `rg -n "burnRateRunsOutInFormat" src/app/shared/i18n.ts` returns matches in `CopyTree`, base copy, and zh-CN copy
    - `rg -n "export const localizeBurnRatePace" src/app/shared/i18n.ts` returns one match
    - `rg -n "export const localizeBurnRateSecondaryLine" src/app/shared/i18n.ts` returns one match
    - `rg -n "Will last until reset|可撑到重置|Runs out in ~|约 .* 后用尽" src/app/shared/i18n.test.ts` returns matches
    - `npx vitest run src/app/shared/i18n.test.ts` passes
  </acceptance_criteria>
  <done>
    Both locales expose short burn-rate labels and secondary-line strings that match the approved UI contract, and the helpers can produce compact confirmation or ETA copy without adding any new date library.
  </done>
</task>

<task type="auto">
  <name>Task 2: Render burn-rate lines inside each quota row and lock the behavior with UI tests</name>
  <files>src/components/panel/QuotaSummary.tsx, src/components/panel/ServiceCard.test.tsx, src/app/panel/PanelView.test.tsx, src/features/demo-services/demoData.ts</files>
  <read_first>
    src/components/panel/QuotaSummary.tsx
    src/components/panel/ServiceCard.tsx
    src/components/panel/ServiceCard.test.tsx
    src/app/panel/PanelView.tsx
    src/app/panel/PanelView.test.tsx
    src/features/demo-services/demoData.ts
    src/lib/tauri/burnRate.ts
    src/lib/tauri/summary.ts
    src/app/shared/i18n.ts
    .planning/phases/04-burn-rate-engine/04-UI-SPEC.md
  </read_first>
  <action>
Modify `src/components/panel/QuotaSummary.tsx` so the quota block order becomes exactly:
`header row -> progress bar -> burn-rate pace line -> burn-rate secondary line -> reset hint`
per D-08 and the UI spec.

Implementation requirements:
- Import the burn-rate helper from `src/lib/tauri/burnRate.ts` or the summary wrapper from `src/lib/tauri/summary.ts`.
- Import `localizeBurnRatePace` and `localizeBurnRateSecondaryLine` from `src/app/shared/i18n.ts`.
- Compute burn-rate at render time using `nowMs` so the existing visible-window minute ticker updates ETA text without creating new samples.
- If the helper returns `undefined`, render no burn-rate block at all per D-11, D-12, and D-13.
- If the helper returns a value, render exactly two new text rows:
  - pace label: `text-xs font-semibold text-slate-700`, sentence case, no uppercase tracking
  - secondary line: `text-xs text-slate-500`
- Keep existing progress bar classes and severity badges untouched; do not alter `progressTone`, `status`, `cardClass`, or `accentClass`.
- Keep reset hint as the lowest-priority line. It remains right-aligned.

Update test fixtures:
- In `src/features/demo-services/demoData.ts`, add `burnRateHistory` to at least the 5h and weekly demo dimensions so the connected demo state exercises both:
  - one dimension that produces `"Will last until reset"`
  - one dimension that produces `"Runs out in ~..."`.
- In `src/components/panel/ServiceCard.test.tsx`, add assertions that:
  - a dimension with valid history shows a pace label and secondary line
  - a dimension with missing history or invalid `resetsAt` does not render either burn-rate line
  - reset hint still renders below the burn-rate copy
- In `src/app/panel/PanelView.test.tsx`, add a regression that a provider with multiple quota rows can display both burn-rate outcomes in the same card without removing the existing `Last refreshed` line.
  </action>
  <verify>
    <automated>npx vitest run src/components/panel/ServiceCard.test.tsx src/app/panel/PanelView.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "localizeBurnRatePace|localizeBurnRateSecondaryLine" src/components/panel/QuotaSummary.tsx` returns matches
    - `rg -n "getBurnRateDisplay" src/components/panel/QuotaSummary.tsx` returns one match
    - `rg -n "text-slate-700" src/components/panel/QuotaSummary.tsx` returns a burn-rate pace line match
    - `rg -n "text-slate-500" src/components/panel/QuotaSummary.tsx` returns a burn-rate secondary line match
    - `rg -n "burnRateHistory" src/features/demo-services/demoData.ts` returns at least 2 matches
    - `rg -n "Will last until reset|Runs out in ~" src/components/panel/ServiceCard.test.tsx src/app/panel/PanelView.test.tsx` returns matches
    - `npx vitest run src/components/panel/ServiceCard.test.tsx src/app/panel/PanelView.test.tsx` passes
  </acceptance_criteria>
  <done>
    Every quota row can render its own two-line burn-rate output inside the existing card layout, degraded rows stay silent, and component tests lock the visible and hidden states without altering current progress-color semantics.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify burn-rate readability and degraded-state silence in the menubar panel</name>
  <files>src/components/panel/QuotaSummary.tsx, src/app/shared/i18n.ts, src/components/panel/ServiceCard.test.tsx, src/app/panel/PanelView.test.tsx</files>
  <read_first>
    .planning/phases/04-burn-rate-engine/04-UI-SPEC.md
    .planning/phases/04-burn-rate-engine/04-VALIDATION.md
    src/components/panel/QuotaSummary.tsx
    src/app/shared/i18n.ts
  </read_first>
  <action>
After Tasks 1 and 2 pass their automated checks, launch the app with representative providers or demo fixtures and verify the visual contract that automation cannot fully prove:
1. Open the panel in `zh-CN` and `en-US`.
2. Confirm a quota row with valid history shows exactly two burn-rate lines: pace label plus secondary line.
3. Confirm rows with insufficient history, missing `resetsAt`, or invalid `resetsAt` show no burn-rate copy at all.
4. Confirm the card still keeps the existing percentage metric, progress bar, reset hint, and last-refreshed line readable at normal menubar width.

Do not add new animations, chips, icons, border stripes, or color coding for burn-rate output during this verification. Any issue found here should be fixed in this plan before approval.
  </action>
  <verify>
    <automated>npx vitest run src/app/shared/i18n.test.ts src/components/panel/ServiceCard.test.tsx src/app/panel/PanelView.test.tsx</automated>
  </verify>
  <how-to-verify>
    1. Run the app in a state with at least one dimension that lasts and one that depletes early.
    2. In `zh-CN`, verify `进度正常` / `可撑到重置` and `消耗偏快` or `消耗过快` / `约 {value} 后用尽` fit within the quota row.
    3. Switch to `en-US` and verify `On track` / `Will last until reset` plus a `Runs out in ~...` case fit within the same layout.
    4. Verify rows lacking history or valid reset timestamps do not show placeholder or broken burn-rate text.
  </how-to-verify>
  <acceptance_criteria>
    - `npx vitest run src/app/shared/i18n.test.ts src/components/panel/ServiceCard.test.tsx src/app/panel/PanelView.test.tsx` passes before manual review
    - Human confirms both locales fit the two-line burn-rate treatment without a third line
    - Human confirms degraded rows render no burn-rate placeholder text
  </acceptance_criteria>
  <resume-signal>Type `approved` if the UI matches the contract, or describe the specific layout/copy issue to fix.</resume-signal>
  <done>
    Manual verification confirms the burn-rate copy remains compact and readable in both locales, and degraded states stay intentionally silent instead of noisy or misleading.
  </done>
</task>

</tasks>

<verification>
Run the phase validation commands from [04-VALIDATION.md](/Users/chasewang/01workspace/projects/ai-usage/.planning/phases/04-burn-rate-engine/04-VALIDATION.md):
- `npx vitest run src/lib/tauri/summary.test.ts src/app/shared/i18n.test.ts src/components/panel/ServiceCard.test.tsx`
- `npx vitest run src/lib/tauri/summary.test.ts src/app/shared/i18n.test.ts src/components/panel/ServiceCard.test.tsx src/app/panel/PanelView.test.tsx src/app/shell/AppShell.test.tsx`
</verification>

<success_criteria>
- `ALERT-01` and `ALERT-02` are visible in the panel per quota dimension.
- Positive and negative burn-rate outcomes use compact localized copy from the approved UI contract.
- Missing or invalid data renders no burn-rate block and no placeholder text.
- Human verification signs off that the menubar panel still fits and remains readable in both locales.
</success_criteria>

<output>
After completion, create `.planning/phases/04-burn-rate-engine/04-02-SUMMARY.md`
</output>
